---
title: Jump-Start Reinforcement Learning with Vision-Language-Action Regularization
title_zh: 利用视觉-语言-动作正则化引导强化学习快速启动
authors: "Angelo Moroncelli, Roberto Zanetti, Marco Maccarini, Loris Roveda"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13733v1"
tags: ["query:1"]
score: 8.0
evidence: 用于机器人操作的视觉-语言-动作模型
tldr: 本文提出VLAJS方法，旨在解决强化学习在处理稀疏奖励和长程机器人操纵任务时探索效率低的问题。该方法将预训练的视觉-语言-动作（VLA）模型作为临时的引导源，通过方向性动作一致性正则化辅助PPO算法进行早期探索。VLA引导采用稀疏触发和逐步退火机制，使智能体在保持高频控制能力的同时，显著提升学习效率并最终超越引导策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13733v1/fig-001.webp\", \"caption\": \"Fig. 7: Learning curves for long-horizon tasks. Sparse RPD makes distillation feasible in long-horizon tasks significantly accelerating convergence compared to PPO baselines. Standard RPD is omitted due to prohibitive training time.\", \"page\": 6, \"index\": 1, \"width\": 529, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2604.13733v1/fig-002.webp\", \"caption\": \"Fig. 8: Learning curves and sample-efficiency comparison for suboptimal reward tasks. VLAJS consistently outperforms PPO and distillation-based baselines— VLAJS (RPD). The initial increase in return is driven by the VLA-based jump-start. Once the VLA guidance is deactivated, the agent transitions to a purely learned policy, which results in a temporary reduction in return while maintaining or improving task success (see the additional material for return plots).\", \"page\": 7, \"index\": 2, \"width\": 1076, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.13733v1/fig-003.webp\", \"caption\": \"TABLE I: Per-task performance reported as Success Rate at t∗ (SR t∗) / Area Under the Success Curve (AUC0..B), both in percent. SR t∗ measures the fraction of evaluation episodes that successfully complete the task within the task-specific time budget t∗ (in environment steps). AUC0..B integrates the success rate over the full episode horizon [0, B], capturing both final performance and learning speed. Bracketed values denote bootstrap 95% confidence intervals over random seeds (n = 6). Bold numbers indicate the best mean performance among the baselines.\", \"page\": 7, \"index\": 3, \"width\": 1014, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.13733v1/fig-004.webp\", \"caption\": \"Fig. 9: Policy robustness under external perturbations and clutter. VLAJS maintains performance where VLA-only policies fail.\", \"page\": 8, \"index\": 4, \"width\": 420, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2604.13733v1/fig-005.webp\", \"caption\": \"Fig. 4: Auxiliary guidance during rollouts. (a) The policy generates actions solely through on-policy exploration at a fixed control frequency, learning both direction and action scale incrementally from reward. (b) A teacher provides continuous action targets throughout the rollout, constraining both direction and magnitude and forcing the policy to match the teacher’s action scale (distillation/RPD style). (c) Guidance is applied sparsely within a rollout and progressively annealed across rollouts, biasing action direction while allowing the policy to learn its own action magnitude and eventually explore freely.\", \"page\": 4, \"index\": 5, \"width\": 526, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.13733v1/fig-006.webp\", \"caption\": \"Fig. 5: Auxiliary losses for VLA-guided RL. (a) Distillation-based methods (e.g., RPD) use an MSE loss that penalizes the full Euclidean distance between policy and teacher actions, constraining both action direction and magnitude. (b) VLAJS instead employs a directional action-consistency loss that penalizes angular misalignment between policy and VLA actions, while remaining invariant to action scale. (c–d) Plots show the superimposition of all training runs across tasks, which consistently follow the same trend: VLA guidance produces a pronounced jump-start, visible as a sharp early bump in rollout reward improvement (the metric used for guidance deactivation), after which the signal remains low as guidance is adaptively turned off.\", \"page\": 5, \"index\": 6, \"width\": 1073, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.13733v1/fig-007.webp\", \"caption\": \"Fig. 6: Simulation and real-world manipulation tasks used in our evaluation. Left: six ManiSkill simulation tasks (PickCube, PickPlaceCube, LiftPegUpright, PegInsertionSide, PokeCube, PushCube). Right: zero-shot real-world deployment on a Franka Panda robot across diverse language-specified tasks.\", \"page\": 5, \"index\": 7, \"width\": 1070, \"height\": 534}]"
motivation: 强化学习在复杂任务中面临探索效率低和信用分配难的问题，而通用的VLA模型虽具备推理能力却难以直接执行高频精确控制。
method: 提出一种跳跃式启动框架，利用VLA模型的稀疏建议对PPO进行动作一致性正则化约束，并在训练过程中动态退火以实现自主优化。
result: "在六项模拟操纵任务中，VLAJS将环境交互需求降低了50%以上，并在真实机器人实验中实现了鲁棒的零样本迁移。"
conclusion: VLAJS通过有效结合VLA的通用推理能力与强化学习的闭环控制优势，显著提升了机器人复杂技能的学习效率和泛化性能。
---

## 摘要
强化学习 (RL) 能够实现机器人操纵的高频闭环控制，但由于探索效率低下和信用分配困难，将其扩展到具有稀疏或不完善奖励的长时程任务仍然具有挑战性。视觉-语言-动作 (VLA) 模型利用大规模多模态预训练来提供通用的任务级推理，但目前的局限性阻碍了其在快速且精确操纵中的直接应用。在本文中，我们提出了视觉-语言-动作引导启动 (VLAJS)，这是一种将稀疏 VLA 指导与同策略 RL 相结合的方法，旨在提高探索和学习效率。VLAJS 将 VLA 视为高层动作建议的瞬态来源，用以引导早期探索并改善信用分配，同时保留了 RL 基于状态的高频控制。我们的方法通过方向性动作一致性正则化增强了近端策略优化 (PPO)，在训练早期使 RL 智能体的动作与 VLA 指导软对齐，而无需强制执行严格模仿、不需要演示数据，也不依赖于持续的教师查询。VLA 指导以稀疏方式应用并随时间退火，使智能体能够在线适应并最终超越指导策略。我们在六个具有挑战性的仿真操纵任务（提升、取放、销钉重定向、销钉插入、拨动和推动物体）中评估了 VLAJS，并在真实的 Franka Panda 机器人上验证了其中一部分任务。VLAJS 在样本效率方面始终优于 PPO 和蒸馏式基准方法，在多个任务中将所需的环境交互减少了 50% 以上。现实世界实验证明了零样本的仿真到现实迁移，以及在杂乱环境、物体变化和外部扰动下的鲁棒执行能力。

## Abstract
Reinforcement learning (RL) enables high-frequency, closed-loop control for robotic manipulation, but scaling to long-horizon tasks with sparse or imperfect rewards remains difficult due to inefficient exploration and poor credit assignment. Vision-Language-Action (VLA) models leverage large-scale multimodal pretraining to provide generalist, task-level reasoning, but current limitations hinder their direct use in fast and precise manipulation. In this paper, we propose Vision-Language-Action Jump-Starting (VLAJS), a method that bridges sparse VLA guidance with on-policy RL to improve exploration and learning efficiency. VLAJS treats VLAs as transient sources of high-level action suggestions that bias early exploration and improve credit assignment, while preserving the high-frequency, state-based control of RL. Our approach augments Proximal Policy Optimization (PPO) with a directional action-consistency regularization that softly aligns the RL agent's actions with VLA guidance during early training, without enforcing strict imitation, requiring demonstrations, or relying on continuous teacher queries. VLA guidance is applied sparsely and annealed over time, allowing the agent to adapt online and ultimately surpass the guiding policy. We evaluate VLAJS on six challenging manipulation tasks: lifting, pick-and-place, peg reorientation, peg insertion, poking, and pushing in simulation, and validate a subset on a real Franka Panda robot. VLAJS consistently outperforms PPO and distillation-style baselines in sample efficiency, reducing required environment interactions by over 50% in several tasks. Real-world experiments demonstrate zero-shot sim-to-real transfer and robust execution under clutter, object variation, and external perturbations.

---

## 论文详细总结（自动生成）

这是一份关于论文《Jump-Start Reinforcement Learning with Vision-Language-Action Regularization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决机器人操纵领域中强化学习（RL）面临的两个核心痛点：**长程任务（Long-horizon）**和**不完善的奖励设计（Imperfect Reward）**导致的探索效率低下和信用分配困难。
*   **背景：** 传统的 RL（如 PPO）在高频闭环控制上表现优异，但在复杂任务中往往需要海量交互且极度依赖奖励工程。
*   **动机：** 现有的视觉-语言-动作（VLA）大模型（如 OpenVLA）具备强大的语义理解和任务推理能力，但由于推理延迟高、控制频率低，难以直接用于精确的实时控制。
*   **核心目标：** 结合 VLA 的“高层智慧”和 RL 的“底层敏捷”，利用 VLA 作为临时的引导源来“跳跃式启动（Jump-Start）”RL 的训练过程。

### 2. 方法论：VLAJS 核心思想与技术细节
论文提出了 **VLAJS (Vision-Language-Action Jump-Starting)** 框架，其核心思想是将 VLA 模型作为一种**稀疏、瞬态的辅助引导**。

*   **稀疏查询与时间离散化：** 为了降低计算开销，系统并不在每个 RL 步都调用 VLA。它仅在每个 Rollout 中进行少量 VLA 查询，并将 VLA 输出的动作增量（Delta Action）通过线性插值或 SLERP 插值分解到后续的 $D$ 个控制步中。
*   **方向性动作一致性损失（Directional Loss）：** 这是本文的关键创新。不同于传统的策略蒸馏（MSE 损失强制匹配动作大小），VLAJS 使用**余弦相似度损失**。它只要求 RL 智能体的动作方向与 VLA 建议一致，而不限制动作的幅值。这允许 RL 智能体在保持 VLA 引导方向的同时，学习更精细、更高频的力度控制。
*   **奖励驱动的跳跃启动机制：** 引导是**瞬态**的。系统会监控奖励的增长趋势，一旦 RL 智能体开始自主获得稳定奖励（如平均奖励超过阈值 3），VLA 引导的强度（损失权重 $\lambda$）和查询频率会按指数规律退火，直至完全关闭。

### 3. 实验设计
*   **场景与任务：** 在 **ManiSkill** 仿真环境中测试了 6 类任务：取方块（PickCube）、取放（PickPlace）、销钉重定向（LiftPegUpright）、销钉插入（PegInsertion）、拨动（Poke）和推动（Push）。
*   **Benchmark：** 针对长程任务（将标准任务步数延长 10 倍）和简化/稀疏奖励任务进行了专门设计。
*   **对比方法：**
    1.  **PPO：** 从零开始训练的标准同策略 RL。
    2.  **Sparse RPD：** 持续使用稀疏 VLA 引导的蒸馏方法（基于 MSE 损失）。
    3.  **VLAJS (RPD)：** 使用本文的跳跃启动机制，但将损失函数替换为 MSE（用于消融实验）。
*   **真实世界验证：** 在 Franka Panda 机器人上进行了零样本（Zero-shot）迁移实验，测试了在杂乱环境和外部扰动下的表现。

### 4. 资源与算力
*   **算力支持：** 论文提到使用了瑞士国家超算中心（CSCS）的 **Alps 系统** 资源。
*   **具体配置：** 虽然未详细列出每个实验的 GPU 确切数量，但提到 VLA 模型（如 OpenVLA）由于参数量大，在训练过程中会带来显著的显存压力和推理延迟，因此“稀疏查询”是实现可行训练的关键。
*   **训练时长：** 实验涉及数百万至数千万步的环境交互（如 PickPlaceCube-v2 达到 37.7M 步）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 6 个仿真任务，每个任务运行了 **6 个随机种子**，并提供了 95% 的置信区间。
*   **消融实验：** 充分对比了不同 VLA 教师（OpenVLA, Octo）、不同相机视角以及不同损失函数（MSE vs. 方向性损失）的影响。
*   **公平性：** 所有对比方法共享相同的 PPO 主干网络、架构和优化器设置，确保了比较的客观性。

### 6. 主要结论与发现
*   **效率提升：** VLAJS 在多个任务中将达到相同成功率所需的交互步数减少了 **50% 以上**。
*   **超越教师：** 即使 VLA 教师本身的成功率不高（如 40%），RL 智能体在引导下最终能达到接近 100% 的成功率，证明了“跳跃启动”优于简单的“模仿”。
*   **鲁棒性：** 训练出的状态空间策略对视觉扰动（如人手干扰、背景变化）比纯视觉 VLA 模型更具鲁棒性。
*   **方向引导的优越性：** 实验证明，在稀疏引导下，只约束动作方向（Directional Loss）比强制匹配动作数值（MSE）更能稳定地加速收敛。

### 7. 优点与亮点
*   **解耦控制频率：** 巧妙解决了 VLA 低频推理与机器人高频控制之间的矛盾。
*   **无需演示数据：** 与传统的模仿学习不同，它不需要预先收集专家轨迹，直接利用现成的预训练模型。
*   **灵活性：** 损失函数的设计非常巧妙，允许智能体在“听取建议”的同时保留自主探索空间，避免了被次优教师带偏。

### 8. 不足与局限
*   **对 VLA 的依赖：** 虽然对 VLA 成功率要求不高，但仍需要 VLA 能提供“至少在方向上正确”的引导。如果 VLA 在某些极端 OOD 任务中完全失效，该方法可能退化为普通 PPO。
*   **计算开销：** 尽管是稀疏查询，但在训练期间挂载一个巨大的 VLA 模型（如 7B 参数）仍比纯 RL 训练需要更多的显存和工程维护成本。
*   **状态估计需求：** 实验中 RL 策略依赖于特权状态（Privileged State），在真实世界部署时需要额外的物体检测器（如 YOLO）来提供状态输入，这增加了系统的复杂性。

（完）
