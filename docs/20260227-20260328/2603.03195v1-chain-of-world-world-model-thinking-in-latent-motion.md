---
title: "Chain of World: World Model Thinking in Latent Motion"
title_zh: 世界链：潜空间运动中的世界模型思维
authors: "Fuxiang Yang, Donglin Di, Lulu Tang, Xuancheng Zhang, Lei Fan, Hao Li, Chen Wei, Tonghua Su, Baorui Ma"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03195v1"
tags: ["query:1"]
score: 9.0
evidence: 将世界模型时间推理与解耦的潜动作表示相结合
tldr: 针对具身智能中视觉语言动作（VLA）模型在预测视觉动态时存在的背景冗余或缺乏时间连续性等问题，本文提出CoWVLA。该模型引入“世界链”范式，利用预训练视频VAE将视频分解为结构和运动潜变量。通过预训练推断连续运动链并预测终端帧，随后在微调阶段将潜动态与离散动作预测对齐。实验证明CoWVLA在机器人仿真任务中优于现有模型，实现了高效的视觉运动学习。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.03195v1/fig-001.webp\", \"caption\": \"Table 1. Comparison of different methods on the LIBERO [32] and SimplerEnv-WidowX [30] benchmarks. The best and the second-best values for each metric are bold and underlined respectively.\", \"page\": 6, \"index\": 1, \"width\": 339, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2603.03195v1/fig-002.webp\", \"caption\": \"Figure 3. Visualization of the disentangled motion and structure latents. We select two frames (t1 and t2) and show the original (Orig.) and reconstructed (Recon.) frames. “M. Recon.” and “S. Recon.” denote the reconstructions obtained by decoding only the motion latent or only the structure latent, respectively. The structure latent preserves the global scene layout, whereas the motion latent captures motion and fine-grained temporal details.\", \"page\": 6, \"index\": 2, \"width\": 1032, \"height\": 935}, {\"url\": \"assets/figures/arxiv/2603.03195v1/fig-003.webp\", \"caption\": \"Figure 4. Cross-reconstruction visualization. We extract the structure latent from the static video in the first row and the motion latent from the robot-arm motion video in the second row. By combining the two latents, we reconstruct the video shown in the third row. We compute the difference between the cross-reconstructed frames and the static frames to highlight the changed regions, which correspond to the robot arm’s motion.\", \"page\": 7, \"index\": 3, \"width\": 1036, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2603.03195v1/fig-004.webp\", \"caption\": \"Figure 5. Comparative visualization of future-frame prediction strategies. There are two tasks demonstrated: i) pick up the black bowl from the table center and place it on the plate, and ii) sweep into a pile. (a) The world-model approach predicts five future frames. (b) The single-goal-frame approach predicts one goal frame. (c) Our method reasons through a learned motion latent zm, producing more reasonable and instruction-aligned frames.\", \"page\": 7, \"index\": 4, \"width\": 1027, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2603.03195v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of the CoWVLA framework. CoWVLA consists of two core components: a latent motion extractor and a VLA decoder. The latent motion extractor, implemented as a video VAE, disentangles each video segment into a structure latent zs and two directional motion latents zhm and zwm, which are concatenated into a unified latent motion vector zm. The VLA decoder performs unified autoregressive modeling over multimodal sequences. During pre-training, the model takes the instruction and initial frame as input, and uses a learnable motion query Q to predict the latent motion ẑm while reconstructing the terminal frame of the video segment. During co-fine-tuning, the input expands into alternating keyframe–action pairs; Q continues to aggregate temporally continuous latent dynamics, guiding multi-step action generation under sparse visual observations.\", \"page\": 3, \"index\": 5, \"width\": 1020, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2603.03195v1/fig-006.webp\", \"caption\": \"Figure 7. Comparison between data collection and real-world deployment during testing.\", \"page\": 18, \"index\": 6, \"width\": 932, \"height\": 763}]"
motivation: 现有的世界模型VLA在重建背景上浪费资源，而潜动作VLA则缺乏时间连续的动态建模和世界知识。
method: 提出CoWVLA范式，通过视频VAE提取运动潜变量，并结合自回归解码器统一建模稀疏关键帧与动作序列。
result: 在机器人仿真基准测试中，CoWVLA的表现超越了现有的世界模型和潜动作方法，并具备良好的计算效率。
conclusion: CoWVLA成功结合了世界模型的时间推理能力与潜动作的简洁性，为具身智能提供了一种更有效的预训练范式。
---

## 摘要
视觉-语言-动作（VLA）模型是通往具身智能的一条极具前景的路径，但它们往往忽略了视觉动态背后的预测性及时间因果结构。世界模型 VLA 通过预测未来帧来解决这一问题，但在重建冗余背景时浪费了模型容量。潜空间动作（Latent-action）VLA 以紧凑的方式编码帧间转换，但缺乏时间连续的动态建模和世界知识。为了克服这些局限性，我们提出了 CoWVLA（Chain-of-World VLA），这是一种全新的“世界链”范式，它将世界模型的时间推理与解耦的潜空间运动表示相结合。首先，预训练的视频 VAE 作为潜空间运动提取器，将视频片段显式地分解为结构潜变量和运动潜变量。随后，在预训练阶段，VLA 根据指令和初始帧学习推断连续的潜空间运动链，并预测该片段的终止帧。最后，在联合微调阶段，通过在统一的自回归解码器中对稀疏关键帧和动作序列进行联合建模，将这种潜空间动态与离散动作预测相对齐。这种设计在保留世界模型在时间推理和世界知识方面优势的同时，兼顾了潜空间动作的紧凑性和可解释性，从而实现了高效的视觉运动学习。在机器人仿真基准测试上的大量实验表明，CoWVLA 优于现有的世界模型和潜空间动作方法，并实现了适度的计算效率，凸显了其作为一种更有效的 VLA 预训练范式的潜力。项目网站见 https://fx-hit.github.io/cowvla-io。

## Abstract
Vision-Language-Action (VLA) models are a promising path toward embodied intelligence, yet they often overlook the predictive and temporal-causal structure underlying visual dynamics. World-model VLAs address this by predicting future frames, but waste capacity reconstructing redundant backgrounds. Latent-action VLAs encode frame-to-frame transitions compactly, but lack temporally continuous dynamic modeling and world knowledge. To overcome these limitations, we introduce CoWVLA (Chain-of-World VLA), a new "Chain of World" paradigm that unifies world-model temporal reasoning with a disentangled latent motion representation. First, a pretrained video VAE serves as a latent motion extractor, explicitly factorizing video segments into structure and motion latents. Then, during pre-training, the VLA learns from an instruction and an initial frame to infer a continuous latent motion chain and predict the segment's terminal frame. Finally, during co-fine-tuning, this latent dynamic is aligned with discrete action prediction by jointly modeling sparse keyframes and action sequences in a unified autoregressive decoder. This design preserves the world-model benefits of temporal reasoning and world knowledge while retaining the compactness and interpretability of latent actions, enabling efficient visuomotor learning. Extensive experiments on robotic simulation benchmarks show that CoWVLA outperforms existing world-model and latent-action approaches and achieves moderate computational efficiency, highlighting its potential as a more effective VLA pretraining paradigm. The project website can be found at https://fx-hit.github.io/cowvla-io.

---

## 论文详细总结（自动生成）

### 论文总结：Chain of World: World Model Thinking in Latent Motion (CoWVLA)

#### 1. 核心问题与研究动机
具身智能中的视觉-语言-动作（VLA）模型目前面临两大挑战：
*   **世界模型（World Model）范式**：通过预测未来像素帧来建模环境动态，但存在大量背景冗余，且离散化后的长序列导致训练效率极低。
*   **潜动作（Latent Action）范式**：虽然能紧凑地编码帧间转换，但通常仅关注两帧之间的变化，缺乏长时间尺度的时间连续性建模，且难以学习到关于“物体如何演变”的世界知识。
**研究动机**：能否构建一种既具备潜动作的紧凑性，又具备世界模型的时间推理能力和物理常识的表示方法？

#### 2. 方法论：CoWVLA 架构与“世界链”范式
CoWVLA 引入了“世界链（Chain of World）”概念，将视频分解为结构和运动的解耦表示。
*   **潜空间运动提取器（Latent Motion Extractor）**：采用预训练的视频 VAE（基于 VidTwin），将视频片段 $V_{1:f}$ 分解为：
    *   **结构潜变量 ($z_s$)**：捕获全局语义和静态场景布局。
    *   **定向运动潜变量 ($z_{hm}, z_{wm}$)**：通过空间平均池化提取高度和宽度方向的动态特征，合并为统一的运动向量 $z_m$。
*   **VLA 解码器（Transformer-decoder）**：基于 8.5B 参数的 Emu3 模型，引入可学习的**运动查询指令（Motion Query, Q）**。
*   **训练阶段**：
    1.  **预训练（Pre-training）**：输入指令和初始帧，让模型通过 $Q$ 预测潜空间运动 $\hat{z}_m$，并预测片段的终止帧（Terminal Frame），建立动态感知的世界先验。
    2.  **联合微调（Co-Fine-Tuning）**：在统一的自回归框架下，交替建模稀疏关键帧和动作序列。$Q$ 充当动态聚合器，引导多步动作生成。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真基准**：LIBERO（包含 Spatial, Object, Goal, Long 四个子集）、SimplerEnv（WidowX 和 Google Robot 场景）、CALVIN（长程操作任务）。
    *   **真实机器人**：使用 Realman RM75B 机械臂进行抓取实验。
*   **对比方法**：
    *   **VLA 基准**：OpenVLA, SpatialVLA, CogACT, $\pi_0$ 等。
    *   **潜动作方法**：LAPA, villa-X, TLA。
    *   **世界模型方法**：WorldVLA, UniVLA, FlowVLA。

#### 4. 资源与算力
论文详细列出了算力消耗：
*   **预训练阶段**：使用 **32 台 A800 GPU**，batch size 为 256，训练 10k 步，耗时约 **24 小时**。
*   **微调阶段**：使用 **16 台 A800 GPU**，batch size 为 128，针对不同基准训练 8k-16k 步，每个配置耗时约 **1-2 天**。
*   **视频 VAE 微调**：使用 4 台 A800 GPU。

#### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多维度评估**：涵盖了从短程空间推理到长程逻辑操作的多种仿真任务，并进行了跨域（SimplerEnv）验证。
*   **消融实验**：深入探讨了潜变量类型（结构 vs 运动）、损失权重（$\lambda_1, \lambda_2$）、关键帧数量 $N$ 以及动作块大小 $l_a$ 对性能的影响。
*   **效率分析**：对比了不同方法在推理速度、显存占用与成功率之间的平衡，证明了 CoWVLA 的高效性。

#### 6. 主要结论与发现
*   **性能领先**：CoWVLA 在 LIBERO (0.956) 和 SimplerEnv (0.760) 上均达到 SOTA，优于纯像素预测的世界模型和纯潜动作方法。
*   **解耦的价值**：显式分离结构和运动特征能显著提升动作推理的准确性，运动潜变量比原始潜变量 $z$ 更具解释性。
*   **世界链效应**：引入终止帧预测（CoT 风格）能显著增强模型对环境演变的感知，从而提升操作成功率。
*   **效率平衡**：相比于 UniVLA 等重型世界模型，CoWVLA 在保持高性能的同时，训练速度更快且显存占用更低。

#### 7. 优点与亮点
*   **创新范式**：首次将“潜动作”与“世界模型预测”统一，提出了“世界链”推理路径。
*   **紧凑且高效**：避免了冗余像素重建，通过预测紧凑的运动向量实现了长程动态建模。
*   **可解释性**：通过可视化证明了模型确实学习到了机械臂的运动轨迹而非简单的背景复制。
*   **鲁棒性**：在真实机器人实验中，模型表现出对光照变化的一定泛化能力。

#### 8. 不足与局限
*   **VAE 依赖性**：潜空间运动的质量高度依赖于预训练视频 VAE 的覆盖范围，若环境与 VAE 训练集差异过大，可能存在分布偏移。
*   **计算开销**：虽然比部分世界模型轻量，但仍基于 8.5B 的大模型，对于实时性要求极高的嵌入式部署仍有挑战。
*   **稀疏观测限制**：虽然支持稀疏关键帧，但在极端动态或遮挡环境下，稀疏采样可能会丢失关键的物理交互瞬间。

（完）
