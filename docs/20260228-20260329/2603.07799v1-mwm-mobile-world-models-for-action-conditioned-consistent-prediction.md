---
title: "MWM: Mobile World Models for Action-Conditioned Consistent Prediction"
title_zh: MWM：用于动作条件一致性预测的移动世界模型
authors: "Han Yan, Zishang Xiang, Zeyu Zhang, Hao Tang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07799v1"
tags: ["query:1"]
score: 10.0
evidence: 用于动作条件一致性预测的移动世界模型
tldr: 本研究针对具身导航中世界模型在多步预测时容易出现动作条件不一致和轨迹漂移的问题，提出了移动世界模型（MWM）。该模型通过两阶段训练框架（结构预训练与动作条件一致性后训练）以及推理一致性状态蒸馏（ICSD）技术，显著提升了长程预测的视觉保真度和轨迹准确性，在保持高效推理的同时增强了规划成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07799v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of the Two-stage training pipeline for MWM. Our training paradigm first performs structure pretraining to learn fine-grained geometry and illumination-dependent appearance, then applies ACC post-training to mitigate compounding error while freezing the CDiT backbone and updating only AdaLN. Within post-training, we introduce ICSD to enable distillation that preserves the consistency objective, while aligning truncated training-time estimates with the inference-time endpoint.\", \"page\": 4, \"index\": 1, \"width\": 1040, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2603.07799v1/fig-002.webp\", \"caption\": \"Fig. 4: Qualitative real-world evaluation. MWM generates planned rollouts that align better with real observations than NWM, indicating reduced multi-step error accumulation and improved planning quality. Blank frames in the figure indicate cases where the robot was forcibly emergency-stopped due to imminent collision.\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2603.07799v1/fig-003.webp\", \"caption\": \"Fig. 5: Real-world deployment setup on the AIRBOT Mobile Manipulation Kit 2 (MMK2). (a) Hardware platform. (b) Deployment process.\", \"page\": 7, \"index\": 3, \"width\": 498, \"height\": 319}]"
motivation: 现有的导航世界模型在多步预测中缺乏动作条件一致性，且高效推理的蒸馏过程往往会破坏这种一致性，导致规划性能下降。
method: 提出了结合动作条件一致性（ACC）后训练的两阶段框架，并引入推理一致性状态蒸馏（ICSD）来优化少步扩散推理过程。
result: 在基准测试和真实世界任务中，MWM在视觉保真度、轨迹精度、规划成功率和推理效率方面均取得了显著提升。
conclusion: MWM通过增强动作一致性和优化蒸馏过程，为基于规划的图像目标导航提供了一个高效且一致的世界模型框架。
---

## 摘要
世界模型允许在想象的未来预测空间中进行规划，为具身导航提供了一个极具前景的框架。然而，现有的导航世界模型往往缺乏动作条件一致性，导致视觉上合理的预测在多步展开（rollout）下仍可能发生漂移，从而降低规划性能。此外，高效部署需要少步扩散推理，但现有的蒸馏方法并未显式地保持展开一致性，造成了训练与推理之间的不匹配。为了应对这些挑战，我们提出了 MWM，一种用于基于规划的图像目标导航的移动世界模型。具体而言，我们引入了一个两阶段训练框架，将结构预训练与动作条件一致性（ACC）后训练相结合，以提高动作条件下的展开一致性。我们还进一步引入了推理一致性状态蒸馏（ICSD），用于具有改进展开一致性的少步扩散蒸馏。我们在基准测试和真实世界任务上的实验表明，在视觉保真度、轨迹准确性、规划成功率和推理效率方面均取得了持续提升。代码：https://github.com/AIGeeksGroup/MWM。网站：https://aigeeksgroup.github.io/MWM。

## 速览
**TLDR**：本研究针对具身导航中世界模型在多步预测时容易出现动作条件不一致和轨迹漂移的问题，提出了移动世界模型（MWM）。该模型通过两阶段训练框架（结构预训练与动作条件一致性后训练）以及推理一致性状态蒸馏（ICSD）技术，显著提升了长程预测的视觉保真度和轨迹准确性，在保持高效推理的同时增强了规划成功率。 \
**Motivation**：现有的导航世界模型在多步预测中缺乏动作条件一致性，且高效推理的蒸馏过程往往会破坏这种一致性，导致规划性能下降。 \
**Method**：提出了结合动作条件一致性（ACC）后训练的两阶段框架，并引入推理一致性状态蒸馏（ICSD）来优化少步扩散推理过程。 \
**Result**：在基准测试和真实世界任务中，MWM在视觉保真度、轨迹精度、规划成功率和推理效率方面均取得了显著提升。 \
**Conclusion**：MWM通过增强动作一致性和优化蒸馏过程，为基于规划的图像目标导航提供了一个高效且一致的世界模型框架。

---

## Abstract
World models enable planning in imagined future predicted space, offering a promising framework for embodied navigation. However, existing navigation world models often lack action-conditioned consistency, so visually plausible predictions can still drift under multi-step rollout and degrade planning. Moreover, efficient deployment requires few-step diffusion inference, but existing distillation methods do not explicitly preserve rollout consistency, creating a training-inference mismatch. To address these challenges, we propose MWM, a mobile world model for planning-based image-goal navigation. Specifically, we introduce a two-stage training framework that combines structure pretraining with Action-Conditioned Consistency (ACC) post-training to improve action-conditioned rollout consistency. We further introduce Inference-Consistent State Distillation (ICSD) for few-step diffusion distillation with improved rollout consistency. Our experiments on benchmark and real-world tasks demonstrate consistent gains in visual fidelity, trajectory accuracy, planning success, and inference efficiency. Code: https://github.com/AIGeeksGroup/MWM. Website: https://aigeeksgroup.github.io/MWM.

---

## 论文详细总结（自动生成）

### MWM: 用于动作条件一致性预测的移动世界模型 —— 论文深度总结

#### 1. 核心问题与研究动机
论文针对具身智能（Embodied AI）导航中的**世界模型（World Models）**存在的两个核心痛点：
*   **动作条件不一致性（Action-Conditioned Inconsistency）**：现有的导航世界模型在进行多步预测（rollout）时，虽然生成的单帧图像视觉上很真实，但随着步数增加，预测的视觉轨迹会偏离动作指令对应的真实物理路径（即误差累积），导致模型预测控制（MPC）规划失败。
*   **推理效率与一致性的矛盾**：为了实时部署，扩散模型需要通过蒸馏减少采样步数，但传统的分布级蒸馏方法无法保证在极少步数（如5步）下依然维持动作与轨迹的一致性。

#### 2. 方法论：核心思想与技术细节
MWM 提出了一个“结构优先，一致性精炼”的两阶段训练框架：
*   **阶段 I：结构预训练（Structure Pretraining）**：
    *   在教师强制（Teacher-forcing）模式下训练条件扩散 Transformer（CDiT），学习环境的精细几何结构、光照和场景动力学。
*   **阶段 II：动作条件一致性（ACC）后训练**：
    *   **核心思想**：让模型在自身生成的预测（Self-conditioned context）中进行训练，而非总是依赖真值（Ground-truth），从而缓解训练与推理时的分布偏移。
    *   **技术细节**：冻结 CDiT 主干网络，仅优化轻量级的 AdaLN 调制层（LoRA 适配器），使用多帧感知损失（LPIPS）来监督预测轨迹与真实观测的对齐。
*   **推理一致性状态蒸馏（ICSD）**：
    *   在后训练中引入“推理一致性状态”（Inference-Consistent State, $s^{IC}$），通过确定性 DDIM 更新对齐训练时的截断估计状态与推理时的最终去噪状态。这使得模型在仅需 5 步采样时，仍能保持极高的一致性。
*   **规划算法**：采用基于交叉熵方法（CEM）的搜索，在世界模型想象的未来空间中寻找使终端帧与目标图像最匹配的动作序列。

#### 3. 实验设计
*   **数据集与场景**：
    *   **SCAND**：大规模人类遥操作导航数据集（包含室内外 8.7 小时数据）。
    *   **MMK2-RealNav**：作者自采的室内导航数据集（1.3km 轨迹，2 小时数据）。
*   **Benchmark 与对比方法**：
    *   对比了 **GNM**、**NoMaD**（反应式策略）以及 **NWM**（现有的导航世界模型）。
    *   评估指标包括：轨迹精度（ATE, RPE）、任务成功率（SR）、导航误差（NE）、视觉质量（FID）和感知一致性（DreamSim, LPIPS）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA RTX PRO 6000 (96GB)** GPU。
*   **训练细节**：
    *   **预训练**：使用 4 块 GPU，总 Batch Size 为 192。
    *   **后训练（ACC）**：仅需 1 块 GPU，进行 1.5K 步优化（约 1 小时内完成），Batch Size 为 2（每样本含 8 步展开）。
*   **推理效率**：在同等硬件上，MWM（5步采样）的平均展开时间为 2.3s，比 NWM（25步采样）的 9.6s 快了 **4 倍以上**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了离线基准测试、消融实验以及真实机器人部署。
*   **消融实验**：分别验证了 ACC 损失函数设计（L1/L2 vs LPIPS）、两阶段训练范式的必要性、以及 ICSD 对减少状态不匹配的作用。
*   **充分性评价**：实验设计较为全面，不仅在公开数据集上验证，还通过 MMK2 机器人进行了实地测试。对比实验考虑了不同采样步数下的性能退化问题，体现了公平性。

#### 6. 主要结论与发现
*   **一致性显著提升**：MWM 在多步展开下的 DreamSim 指标降低了 20.4%，ATE 降低了 10.9%，证明了 ACC 后训练能有效抑制误差累积。
*   **高效且高质量**：MWM 在仅用 5 步采样的情况下，视觉保真度（FID）甚至优于 25 步采样的 NWM，实现了速度与质量的双赢。
*   **实战表现优异**：在真实世界导航任务中，MWM 相比 NWM 实现了 50% 的相对成功率提升，导航误差降低了 32.1%。

#### 7. 优点与亮点
*   **训练策略巧妙**：通过仅微调 AdaLN 层而非全量微调，既保留了预训练的结构知识，又快速习得了动作一致性。
*   **解决实际痛点**：ICSD 机制直接针对扩散模型在机器人实时规划中“采样慢”和“蒸馏后不准”的矛盾，具有很强的工程实用价值。
*   **端到端验证**：从算法理论到仿真基准，再到复杂室内环境的真机部署，闭环验证完整。

#### 8. 不足与局限
*   **开环规划限制**：目前的系统主要执行开环、单次规划（One-shot planning），即规划好序列后直接执行，缺乏在线重规划（Replanning）能力，难以应对动态障碍物。
*   **感知模态单一**：实验主要依赖 RGB 图像，未充分利用 LiDAR 或深度信息，在极端光照或纹理缺失环境下可能存在鲁棒性风险。
*   **计算延迟**：虽然提速 4 倍，但 2.3s 的展开时间对于高频控制（如 >10Hz）仍显略慢，尚需进一步优化。

（完）
