---
title: Discrete World Models via Regularization
title_zh: 通过正则化实现的离散世界模型
authors: "Davide Bizzaro, Luciano Serafini"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01748v1"
tags: ["query:1"]
score: 9.0
evidence: 用于规划的无监督布尔世界模型学习
tldr: 本研究提出了一种名为DWMR的离散世界模型，旨在通过正则化手段学习环境的布尔状态表示。与传统的基于重构或对比学习的方法不同，DWMR采用无重构、无对比的无监督学习方式，通过方差、相关性和偏度惩罚来最大化表示位的熵和独立性，并引入局部性先验。实验证明，该方法在组合结构基准测试中优于重构模型，能更准确地捕捉状态转换，为符号推理和规划提供了高效的潜在空间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.01748v1/fig-001.webp\", \"caption\": \"Table 1: Mean per-cell f1-scores, for reconstructing the board configurations from encoder states (Enc.) and from one-step roll-outs in imagination (Im.), using linear probes. We report the mean and standard deviation over 10 different runs, for each model and benchmark.\", \"page\": 11, \"index\": 1, \"width\": 629, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2603.01748v1/fig-002.webp\", \"caption\": \"Fig. 1: Overview of the model architecture and of the loss function. Encoders map successive observations into a shared Boolean latent space, and a predictor transforms the current latent state into the next, given the action. We illustrate and evaluate this setup on an 8-puzzle benchmark with MNIST digits, where actions move the blank tile and induce local state changes and new renderings of the digits. The crossed lines denote operations that stop the gradient, and we introduce a training scheme with two-steps updates: initially, the predictor is updated solely based on hard bits b, followed by a joint update of the encoder and predictor using probabilities p. Test-time inference relies only on the pathway involving b. The parameters ϕ′ are an EMA copy of the parameters ϕ.\", \"page\": 4, \"index\": 2, \"width\": 696, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2603.01748v1/fig-003.webp\", \"caption\": \"Table 2: Mean per-cell accuracies, for reconstructing the board configurations from encoder states and from one-step roll-outs in imagination, using linear probes. We report the mean and standard deviation over 10 different runs on the MNIST 8-puzzle benchmark.\", \"page\": 12, \"index\": 3, \"width\": 652, \"height\": 240}]"
motivation: 现有的世界模型多依赖重构或对比信号，难以在无监督下高效学习适用于符号推理的离散布尔表示。
method: 提出DWMR方法，通过结合潜在预测与专门的正则化项（如方差、相关性和偏度惩罚）以及局部性先验来学习离散表示。
result: 在具有组合结构的基准测试中，DWMR在表示准确性和状态转换预测方面均优于基于重构的基准模型。
conclusion: 通过正则化而非重构来约束潜在空间，可以更有效地学习离散世界模型，为复杂环境下的规划和推理奠定基础。
---

## 摘要
世界模型旨在将环境的状态和动力学捕捉到紧凑的潜空间中。此外，使用布尔状态表示对于搜索启发式以及符号推理和规划特别有用。现有方法通过基于解码器的重构，或者通过对比或奖励信号来保持潜变量的信息量。在这项工作中，我们提出了通过正则化实现的离散世界模型（DWMR）：一种用于无监督布尔世界模型学习的无需重构且无需对比的方法。具体而言，我们引入了一种新颖的世界建模损失，将潜变量预测与专门的正则化项相结合。这些正则化项通过方差、相关性和共偏度（coskewness）惩罚来最大化表示位的熵和独立性，同时为稀疏动作变化强制执行局部性先验。为了实现有效的优化，我们还引入了一种新颖的训练方案，提高了对离散展开（roll-outs）的鲁棒性。在两个具有底层组合结构的基准测试上的实验表明，DWMR 比基于重构的替代方案学习到更准确的表示和转换。最后，DWMR 还可以与辅助重构解码器配对，这种组合会产生额外的收益。

## 速览
**TLDR**：本研究提出了一种名为DWMR的离散世界模型，旨在通过正则化手段学习环境的布尔状态表示。与传统的基于重构或对比学习的方法不同，DWMR采用无重构、无对比的无监督学习方式，通过方差、相关性和偏度惩罚来最大化表示位的熵和独立性，并引入局部性先验以确保动作引起的稀疏变化。实验证明，该方法在组合结构基准测试中优于重构模型，能更准确地捕捉状态转换，为符号推理和规划提供了高效的潜在空间。 \
**Motivation**：现有的世界模型多依赖重构或对比信号，难以在无监督下高效学习适用于符号推理和搜索启发式的离散布尔表示。 \
**Method**：提出DWMR方法，通过结合潜在预测与专门的正则化项（如方差、相关性和偏度惩罚）以及局部性先验来学习离散表示，并引入了增强离散演化鲁棒性的训练方案。 \
**Result**：在具有组合结构的基准测试中，DWMR在表示准确性和状态转换预测方面均优于基于重构的基准模型，且可与辅助解码器结合进一步提升性能。 \
**Conclusion**：通过正则化而非重构来约束潜在空间，可以更有效地学习离散世界模型，为复杂环境下的规划和推理任务提供了更优的表征基础。

---

## Abstract
World models aim to capture the states and dynamics of an environment in a compact latent space. Moreover, using Boolean state representations is particularly useful for search heuristics and symbolic reasoning and planning. Existing approaches keep latents informative via decoder-based reconstruction, or instead via contrastive or reward signals. In this work, we introduce Discrete World Models via Regularization (DWMR): a reconstruction-free and contrastive-free method for unsupervised Boolean world-model learning. In particular, we introduce a novel world-modeling loss that couples latent prediction with specialized regularizers. Such regularizers maximize the entropy and independence of the representation bits through variance, correlation, and coskewness penalties, while simultaneously enforcing a locality prior for sparse action changes. To enable effective optimization, we also introduce a novel training scheme improving robustness to discrete roll-outs. Experiments on two benchmarks with underlying combinatorial structure show that DWMR learns more accurate representations and transitions than reconstruction-based alternatives. Finally, DWMR can also be paired with an auxiliary reconstruction decoder, and this combination yields additional gains.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DWMR (Discrete World Models via Regularization)** 的新型离散世界模型构建方法。以下是对该论文的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无监督的情况下，从高维观测（如图像）中学习到紧凑、信息丰富且具有良好动力学特性的**布尔（Boolean）潜在状态表示**。
*   **研究背景**：
    *   离散/布尔表示对于符号推理、启发式搜索和规划至关重要，因为它们能精确识别状态并减少模型漂移。
    *   现有方法通常依赖**像素级重构**（易关注噪声细节而非核心逻辑）或**对比学习**（依赖数据增强和负样本）。
    *   论文旨在探索：能否仅通过**正则化**手段，在不使用解码器和对比学习的情况下，诱导出高质量的布尔世界模型？

### 2. 方法论：核心思想与技术细节
DWMR 采用了一种**联合嵌入预测架构（JEPA）**，其核心是通过精心设计的损失函数来约束布尔潜空间。

*   **核心架构**：
    *   **编码器 ($enc_\phi$)**：将图像映射为 Bernoulli 概率向量 $p$，并通过 0.5 阈值得到硬比特 $b$。
    *   **预测器 ($pred_\psi$)**：根据当前状态和动作预测下一时刻的概率 $\hat{p}'$。
    *   **目标编码器 ($enc_{\phi'}$)**：使用在线编码器的指数移动平均（EMA）副本，提供稳定的目标比特 $b'$。
*   **关键损失函数 ($L_{DWMR}$)**：
    1.  **预测损失 ($L_{pred}$)**：预测概率与目标比特之间的二元交叉熵。
    2.  **方差正则化 ($L_{var}$)**：强制每个比特的标准差达到一定阈值，防止所有比特塌缩为常数。
    3.  **相关性正则化 ($L_{cor}$)**：惩罚比特间的互相关，减少冗余，促进维度独立。
    4.  **共偏度正则化 ($L_{cos}$)**：惩罚三阶交叉矩，消除二阶相关性无法捕捉的高阶依赖。
    5.  **局部性正则化 ($L_{loc}$)**：引入结构先验，即一个动作通常只改变少数比特（稀疏性），通过惩罚翻转比特数偏离预设范围来实现。
*   **训练方案**：采用**两步更新法**。第一步固定编码器，仅用硬比特输入训练预测器（模拟测试环境）；第二步联合更新编码器和预测器，使用连续概率进行反向传播。

### 3. 实验设计
*   **实验场景/数据集**：
    *   **MNIST 8-puzzle**：3x3 滑动拼图，瓷砖由 MNIST 数字组成，动作移动空格。测试模型对组合状态和未见数字的泛化能力。
    *   **IceSlider**：8x8 网格拼图，智能体在冰面上滑动直到撞墙。
    *   **噪声设置**：在上述任务中加入高斯像素噪声，测试鲁棒性。
*   **评估指标**：使用**线性探针（Linear Probes）**。冻结编码器，训练一个线性层尝试从布尔向量中恢复原始符号状态（如拼图的具体布局），计算 F1 分数或准确率。
*   **对比方法（Baselines）**：
    *   **AE**（基于重构的自编码器）。
    *   **$\beta$-VAE**（变分自编码器）。
    *   **DeepCubeAI**（一种先进的离散自编码世界模型）。
    *   **DWMR 变体**（如加入辅助解码器的 DWMR+AE）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 Adam 优化器，Batch Size 为 256，MNIST 8-puzzle 训练 40 个 epoch，IceSlider 训练 20 个 epoch。代码已在 GitHub 开源。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的组合基准测试上进行了实验，每个实验均包含“清洁”和“多噪”两种环境。
*   **消融实验**：非常充分。作者逐一移除了方差、相关性、共偏度、局部性正则化项以及 EMA 机制，验证了每一项对防止塌缩和提高表示质量的必要性。
*   **客观性**：每个模型均进行了超参数搜索，并报告了 10 次独立运行的均值和标准差，确保了结果的统计显著性和公平性。

### 6. 主要结论与发现
*   **正则化的有效性**：DWMR 在不使用像素重构的情况下，学习到的状态表示和动力学模型准确率显著优于基于重构的基准模型（如 DeepCubeAI）。
*   **防止塌缩**：方差正则化是防止潜空间塌缩的最关键因素；相关性和共偏度正则化对于学习解耦的特征至关重要。
*   **局部性先验的作用**：引入动作引起状态局部变化的先验，能显著提升模型在噪声环境下的鲁棒性。
*   **互补性**：虽然 DWMR 不需要解码器，但如果加上辅助解码器（DWMR+AE），性能会进一步提升。

### 7. 优点
*   **创新性**：首次将 JEPA 理念与布尔正则化结合，摆脱了对复杂解码器或对比样本的依赖。
*   **高阶约束**：引入共偏度（Coskewness）来处理比特间的高阶依赖，这在以往的表示学习中较少见。
*   **实用性**：生成的布尔向量直接兼容现有的符号规划器和搜索算法。

### 8. 不足与局限
*   **环境限制**：目前仅在全观测、确定性的离散动作环境中验证，尚未测试其在部分观测（POMDP）或随机性环境中的表现。
*   **超参数敏感性**：正则化项较多（5个权重），且需要配合复杂的超参数调度（Scheduling），调参成本可能较高。
*   **任务范围**：实验主要集中在拼图类组合任务，对于视觉特征极其复杂或连续控制任务的适用性有待验证。

（完）
