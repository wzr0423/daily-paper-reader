---
title: What Do World Models Learn in RL? Probing Latent Representations in Learned Environment Simulators
title_zh: 世界模型在强化学习中学习到了什么？探究学习环境模拟器中的潜表征
authors: Xinyu Zhang
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21546v1"
tags: ["query:1"]
score: 9.0
evidence: 探究强化学习世界模型中的潜空间表示
tldr: 本研究探讨了强化学习中世界模型（如基于Transformer的IRIS和基于扩散模型的DIAMOND）在模拟环境时内部学习到的表示。通过线性探测、因果干预和注意力分析，研究发现这些模型在Atari游戏中自发形成了线性可解码的游戏状态表示（如物体位置、得分）。因果干预证明了这些表示具有功能性，而注意力分析揭示了模型对关键游戏对象的空间关注，为理解世界模型的内部机制提供了实证支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21546v1/fig-001.webp\", \"caption\": \"Table 1: Best-layer R2 (mean ± std over 5-fold CV) for Breakout. Both linear and MLP probes are shown; the small selectivity gap (∆) confirms approximately linear representations.\", \"page\": 3, \"index\": 1, \"width\": 722, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.21546v1/fig-002.webp\", \"caption\": \"Figure 1: Probe R2 across layers (in network data-flow order) for IRIS (left) and DIAMOND (right) on Breakout (top) and Pong (bottom). Each line tracks one game-state property; shaded bands show ±1 std over 5-fold CV. IRIS representations are flat across transformer layers, while DIAMOND shows a peaked inverted-V centered on the UNet bottleneck. Note: y-axis includes negative R2 values, revealing that DIAMOND’s early encoder layers are worse than a constant predictor for ball position.\", \"page\": 3, \"index\": 2, \"width\": 816, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2603.21546v1/fig-003.webp\", \"caption\": \"Figure 3: Three-way token ablation on Breakout (4× 4 grid). Zero, mean, and random replacement produce consistent importance rankings (ρ > 0.92), with token 0 (score/brick region) most critical.\", \"page\": 4, \"index\": 3, \"width\": 772, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.21546v1/fig-004.webp\", \"caption\": \"Figure 2: Causal intervention on Breakout: shifting IRIS layer-5 hidden states along probe directions produces correlated changes in predictions (r ≥ 0.96 for all properties, measured via KL divergence).\", \"page\": 4, \"index\": 4, \"width\": 826, \"height\": 237}]"
motivation: 旨在揭示世界模型在模拟环境动态时，其内部隐藏层究竟学习并表示了哪些结构化信息。
method: 对IRIS（离散Token Transformer）和DIAMOND（连续扩散UNet）两种架构应用了线性/非线性探测、因果干预及注意力分析。
result: 发现模型内部存在线性可解码的游戏状态变量，且因果干预证实这些表示直接影响模型预测，同时注意力机制表现出明显的空间特异性。
conclusion: 实验证明不同架构的世界模型均能自发学习到结构化且近似线性的环境状态内部表示，并将其用于模拟预测。
---

## 摘要
世界模型通过经验学习模拟环境动力学，从而实现高样本效率的强化学习。但这些模型内部究竟表征了什么？我们将可解释性技术（包括线性与非线性探测、因果干预和注意力分析）应用于两种架构截然不同的世界模型：IRIS（离散令牌 Transformer）和 DIAMOND（连续扩散 UNet），并在 Atari 的 Breakout 和 Pong 游戏上进行了训练。利用线性探测，我们发现两种模型都形成了游戏状态变量（如物体位置、得分）的线性可解码表征，而 MLP 探测仅产生略高的 R^2，这证实了这些表征近似线性。因果干预——沿探测导出的方向偏移隐藏状态——在模型预测中产生了相关的变化，证明了这些表征是被功能性地使用，而不仅仅是具有相关性。对 IRIS 注意力头的分析揭示了空间专业化：特定的注意力头会优先关注与游戏物体重叠的令牌。多基准令牌消融实验一致表明，包含物体的令牌具有不成比例的重要性。我们的研究结果提供了可解释性证据，表明在两种游戏和两种架构中，学习到的世界模型都形成了结构化的、近似线性的环境状态内部表征。

## Abstract
World models learn to simulate environment dynamics from experience, enabling sample-efficient reinforcement learning. But what do these models actually represent internally? We apply interpretability techniques--including linear and nonlinear probing, causal interventions, and attention analysis--to two architecturally distinct world models: IRIS (discrete token transformer) and DIAMOND (continuous diffusion UNet), trained on Atari Breakout and Pong. Using linear probes, we find that both models develop linearly decodable representations of game state variables (object positions, scores), with MLP probes yielding only marginally higher R^2, confirming that these representations are approximately linear. Causal interventions--shifting hidden states along probe-derived directions--produce correlated changes in model predictions, providing evidence that representations are functionally used rather than merely correlated. Analysis of IRIS attention heads reveals spatial specialization: specific heads attend preferentially to tokens overlapping with game objects. Multi-baseline token ablation experiments consistently identify object-containing tokens as disproportionately important. Our findings provide interpretability evidence that learned world models develop structured, approximately linear internal representations of environment state across two games and two architectures.

---

## 论文详细总结（自动生成）

这是一份关于论文《What Do World Models Learn in RL? Probing Latent Representations in Learned Environment Simulators》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：强化学习中的“世界模型”（World Models）在通过经验学习模拟环境动力学时，其内部隐藏层究竟表征了什么信息？
*   **研究背景**：虽然如 IRIS 和 DIAMOND 等模型在 Atari 100k 基准上表现卓越，但其内部表征的机制仍是黑盒。研究者试图验证“线性表征假设”（Linear Representation Hypothesis）在视觉强化学习模拟器中是否成立，即模型是否自发学习到了结构化、可线性解码的游戏状态信息（如球的位置、得分等）。

### 2. 方法论：核心思想与技术细节
研究者采用了机械可解释性（Mechanistic Interpretability）中的四种关键技术：
*   **线性与非线性探测（Probing）**：
    *   使用岭回归（Ridge Regression）作为线性探测器，使用 2 层 MLP 作为非线性探测器。
    *   通过比较两者的 $R^2$ 差距（$\Delta = R^2_{MLP} - R^2_{linear}$）来衡量表征的线性程度。
*   **因果干预（Causal Interventions）**：
    *   采用激活补丁（Activation Patching）技术。
    *   沿着探测器学到的权重方向 $\hat{w}$ 偏移隐藏状态 $h' = h + \alpha \cdot \hat{w}$，观察模型预测输出（KL 散度、Token 变化率）的变化，以验证表征是否具有功能性（即模型是否真的利用了这些信息进行预测）。
*   **注意力分析（Attention Analysis）**：
    *   计算 IRIS 模型中 40 个注意力头的熵和空间选择性，分析不同头是否专注于特定的游戏区域或对象。
*   **令牌消融（Token Ablation）**：
    *   通过零替换、均值替换和随机代码本替换三种基准，移除特定空间位置的 Token，测量其对预测破坏的程度。

### 3. 实验设计与对比
*   **实验场景**：Atari 游戏中的 **Breakout**（打砖块）和 **Pong**（乒乓球）。
*   **研究对象（模型架构）**：
    1.  **IRIS**：基于离散 Token 的 Transformer 架构（VQ-VAE + GPT-2）。
    2.  **DIAMOND**：基于连续扩散模型的 UNet 架构。
*   **基准（Ground Truth）**：从 Atari RAM 中提取的真实状态变量（球的 x/y 坐标、玩家位置、得分等）。
*   **对比基准（Baselines）**：原始像素（Raw Pixels）、随机初始化模型、打乱标签（Shuffled Labels）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **数据规模**：实验针对每个游戏提取了 10,000 帧图像进行探测分析。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了两种截然不同的主流架构（Transformer vs. Diffusion）和两个经典游戏环境。
*   **充分性**：
    *   进行了 5 折交叉验证以确保 $R^2$ 结果的稳定性。
    *   使用了多种消融基准（零、均值、随机）来验证 Token 重要性的排名一致性（相关系数 $\rho > 0.9$）。
    *   因果干预实验通过相关系数 $r > 0.95$ 强力证明了结论。
*   **客观性**：通过对比原始像素和随机模型，排除了表征是由于输入数据本身简单而产生的错觉。

### 6. 主要结论与发现
*   **线性表征的普遍性**：两种架构均自发形成了近似线性的游戏状态表征。IRIS 的 $\Delta \le 0.06$，DIAMOND 在瓶颈层 $\Delta = 0.04$。
*   **架构差异**：
    *   **IRIS**：表征在 Transformer 各层中非常平稳，VQ-VAE 编码器已经完成了大部分状态提取工作。
    *   **DIAMOND**：呈现“倒 V 型”特征，抽象状态在 UNet 的瓶颈层压缩最剧烈，线性度最高。
*   **功能性验证**：干预实验证明，修改这些线性方向会直接且成比例地影响模型的预测逻辑。
*   **空间分工**：IRIS 的注意力头存在空间专业化，能够区分并追踪不同的游戏元素。

### 7. 优点与亮点
*   **跨架构验证**：证明了线性表征是世界模型的通用属性，而非特定架构（如 Transformer）的专利。
*   **超越相关性**：通过因果干预实验，有力地反驳了“探测器只是在寻找无用相关性”的质疑。
*   **多维度分析**：结合了宏观的探测分析和微观的注意力/Token 消融分析，提供了完整的解释链条。

### 8. 不足与局限
*   **环境复杂度有限**：实验仅限于 2D Atari 游戏，尚未在更复杂的 3D 环境或具有部分可观测性的任务中验证。
*   **时间维度缺失**：单帧探测可能忽略了 Transformer 编码的跨帧时间动力学信息。
*   **干预手段较粗糙**：沿单一线性方向偏移是一种粗略的干预，未来可能需要更精细的因果抽象方法。

（完）
