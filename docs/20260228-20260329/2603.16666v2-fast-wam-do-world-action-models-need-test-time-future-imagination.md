---
title: "Fast-WAM: Do World Action Models Need Test-time Future Imagination?"
title_zh: Fast-WAM：世界动作模型在测试时需要未来想象吗？
authors: "Tianyuan Yuan, Zibin Dong, Yicheng Liu, Hang Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16666v2"
tags: ["query:1"]
score: 10.0
evidence: 用于具身控制的世界动作模型 (WAM)
tldr: 本研究探讨了世界动作模型（WAM）在推理阶段是否需要显式的未来想象。针对现有模型因迭代视频去噪导致的高延迟问题，本文提出 Fast-WAM，在训练时保留视频协同训练以增强表征，但在推理时跳过未来预测。实验表明，Fast-WAM 在 LIBERO 和 RoboTwin 等基准测试中表现优异，推理速度提升 4 倍以上，证明了视频预测的主要价值在于训练阶段的表征学习而非推理时的生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.16666v2/fig-001.webp\", \"caption\": \"Figure 1: Three representative WAM paradigms. (A) Joint-modeling WAMs denoise future video and action tokens together. (B) Causal WAMs first generate future observations and then condition action prediction on the generated future representation. (C) Fast-WAM retains video co-training during training but removes explicit future generation at inference time, directly predicting actions from latent world representations in a single forward pass.\", \"page\": 2, \"index\": 1, \"width\": 808, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2603.16666v2/fig-002.webp\", \"caption\": \"Table 1: Results on RoboTwin. Fast-WAM matches strong pretrained WAM baselines without using embodied pretraining, while the two imagine-then-execute variants remain highly comparable and removing video co-training causes a substantial drop.\", \"page\": 7, \"index\": 2, \"width\": 656, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2603.16666v2/fig-003.webp\", \"caption\": \"Figure 3: Real-world towel-folding task on the Galaxea R1 Lite platform. Folding a deformable object requires long-horizon planning and precise closed-loop manipulation, making it a challenging benchmark for evaluating both task success and execution efficiency.\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.16666v2/fig-004.webp\", \"caption\": \"Table 3: Per-task success rates on RoboTwin under clean and randomized evaluation settings.\", \"page\": 13, \"index\": 4, \"width\": 822, \"height\": 932}, {\"url\": \"assets/figures/arxiv/2603.16666v2/fig-005.webp\", \"caption\": \"Table 2: Results on LIBERO. Fast-WAM achieves competitive overall performance without embodied pretraining, remains close to both imagine-then-execute variants, and outperforms the no-video-cotraining ablation by a clear margin.\", \"page\": 8, \"index\": 5, \"width\": 776, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2603.16666v2/fig-006.webp\", \"caption\": \"Figure 4: Real-world results on the long-horizon towel-folding task. The left panel plots success rate against average completion time, where upper-left is better. The right panel compares inference latency. Fast-WAM achieves strong real-world performance with substantially lower latency than imagine-then-execute variants, while removing video co-training degrades both success rate and completion time.\", \"page\": 9, \"index\": 6, \"width\": 771, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2603.16666v2/fig-007.webp\", \"caption\": \"Figure 2: Fast-WAM architecture and the structured attention mask used to disentangle video cotraining from action generation.\", \"page\": 5, \"index\": 7, \"width\": 715, \"height\": 413}]"
motivation: 现有世界动作模型依赖推理时的视频生成导致高延迟，且不确定显式未来想象对动作性能是否必要。
method: 提出 Fast-WAM 架构，在训练中结合视频建模与动作预测，但在推理阶段仅执行动作预测以提高效率。
result: Fast-WAM 在仿真和真实任务中达到领先水平，推理延迟仅 190ms，比传统 WAM 快 4 倍以上。
conclusion: 视频预测在 WAM 中的核心价值在于训练期间优化世界表征，而非推理时的显式未来观测生成。
---

## 摘要
世界动作模型 (WAMs) 已成为具身控制中视觉-语言-动作 (VLA) 模型的一种极具前景的替代方案，因为它们显式地建模了视觉观测在动作作用下如何演化。大多数现有的 WAM 遵循“先想象后执行”的范式，由于迭代视频去噪导致了显著的测试时延迟，然而目前尚不清楚显式的未来想象对于强大的动作性能是否真的必要。在本文中，我们探讨了 WAM 在测试时是否需要显式的未来想象，或者其优势是否主要源于训练期间的视频建模。通过提出 Fast-WAM，我们将训练期间的视频建模作用与推理期间的显式未来生成解耦。Fast-WAM 是一种在训练时保留视频协同训练但在测试时跳过未来预测的 WAM 架构。我们进一步实例化了多个 Fast-WAM 变体，以便对这两个因素进行受控比较。在这些变体中，我们发现 Fast-WAM 与“先想象后执行”变体相比仍具竞争力，而移除视频协同训练则会导致更大幅度的性能下降。实验表明，在无需具身预训练的情况下，Fast-WAM 在仿真基准测试（LIBERO 和 RoboTwin）以及现实世界任务中均取得了与最先进方法相当的结果。其运行延迟为 190ms，实现了实时运行，比现有的“先想象后执行”型 WAM 快 4 倍以上。这些结果表明，WAM 中视频预测的主要价值可能在于提高训练期间的世界表示能力，而非在测试时生成未来观测。项目主页：https://yuantianyuan01.github.io/FastWAM/

## Abstract
World Action Models (WAMs) have emerged as a promising alternative to Vision-Language-Action (VLA) models for embodied control because they explicitly model how visual observations may evolve under action. Most existing WAMs follow an imagine-then-execute paradigm, incurring substantial test-time latency from iterative video denoising, yet it remains unclear whether explicit future imagination is actually necessary for strong action performance. In this paper, we ask whether WAMs need explicit future imagination at test time, or whether their benefit comes primarily from video modeling during training. We disentangle the role of video modeling during training from explicit future generation during inference by proposing \textbf{Fast-WAM}, a WAM architecture that retains video co-training during training but skips future prediction at test time. We further instantiate several Fast-WAM variants to enable a controlled comparison of these two factors. Across these variants, we find that Fast-WAM remains competitive with imagine-then-execute variants, while removing video co-training causes a much larger performance drop. Empirically, Fast-WAM achieves competitive results with state-of-the-art methods both on simulation benchmarks (LIBERO and RoboTwin) and real-world tasks, without embodied pretraining. It runs in real time with 190ms latency, over 4$\times$ faster than existing imagine-then-execute WAMs. These results suggest that the main value of video prediction in WAMs may lie in improving world representations during training rather than generating future observations at test time. Project page: https://yuantianyuan01.github.io/FastWAM/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-WAM: Do World Action Models Need Test-time Future Imagination?》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：世界动作模型（World Action Models, WAMs）在推理阶段是否真的需要显式地生成未来视频（即“未来想象”）？
*   **背景**：现有的 WAM 普遍采用“先想象后执行”范式，通过迭代去噪生成未来视觉观测来辅助动作预测。虽然效果显著，但这种方式带来了巨大的计算延迟（通常需数秒），难以满足实时控制需求。
*   **研究假设**：作者怀疑 WAM 的性能提升主要源于训练时的“视频协同训练（Video Co-training）”帮助模型学习了更好的物理表征，而非推理时生成的具体图像。

### 2. 方法论：Fast-WAM
*   **核心思想**：**解耦训练与推理**。在训练阶段保留视频预测任务以强化表征学习，但在推理阶段完全跳过视频生成，直接从潜在世界表征中预测动作。
*   **架构设计**：
    *   基于 **Wan2.2-5B** 视频扩散 Transformer (DiT) 作为骨干网络。
    *   采用 **Mixture-of-Transformer (MoT)** 架构，包含一个视频 DiT 分支和一个 1B 参数的动作专家（Action Expert）分支。
    *   **结构化注意力掩码**：在训练时，动作 Token 可以访问当前帧的视觉 Token，但被禁止访问未来的视频 Token，从而确保推理时即使没有未来视频，动作预测逻辑依然一致。
*   **算法流程**：
    *   **训练**：使用联合流匹配（Flow Matching）目标函数，同时优化动作预测损失（$L_{act}$）和视频建模损失（$L_{vid}$）。
    *   **推理**：仅执行一次视频骨干网络的单向传递（Single Forward Pass）提取特征，随后由动作专家直接生成动作序列，无需迭代去噪生成视频。

### 3. 实验设计
*   **实验场景与基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务。
    *   **RoboTwin 2.0**：具有挑战性的双臂协作基准，包含 50 多个任务，涉及大量场景随机化。
    *   **真实世界任务**：长程折叠毛巾任务，评估成功率和执行效率（完成时间）。
*   **对比方法**：
    *   **VLA 基线**：OpenVLA, $\pi_0$, $\pi_{0.5}$。
    *   **WAM 基线**：Motus, LingBot-VA。
    *   **受控变体（消融实验）**：
        1.  **Fast-WAM-Joint**：推理时联合去噪视频和动作。
        2.  **Fast-WAM-IDM**：先生成视频再预测动作（传统 WAM 模式）。
        3.  **无视频协同训练版本**：仅保留架构，移除训练时的视频预测目标。

### 4. 资源与算力
*   **硬件**：推理延迟在单张 **NVIDIA RTX 5090D V2 (32GB)** 上测量。
*   **训练细节**：
    *   LIBERO 训练 20k 步；RoboTwin 和真实世界任务训练 30k 步。
    *   使用 AdamW 优化器，学习率 $1 \times 10^{-4}$。
*   **算力规模**：文中未明确提及训练总时长或使用的 GPU 总数（如集群规模），但提到了模型总参数量约为 6B。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 90 多个任务，进行了数千次评测（如 LIBERO 2000 次试验，RoboTwin 每任务 100 次试验）。
    *   真实世界实验包含 60 小时的演示数据。
*   **充分性评价**：实验设计非常**客观且公平**。通过构建具有相同骨干网络的“受控变体”，作者成功排除了模型容量、预训练数据等干扰因素，直接验证了“推理时想象”与“训练时协同”的相对贡献。

### 6. 主要结论与发现
*   **性能与速度双赢**：Fast-WAM 在不使用具身预训练的情况下，达到了与最强预训练 WAM 相当的水平，且推理延迟仅为 **190ms**，比传统 WAM 快 **4 倍以上**。
*   **核心发现**：**视频协同训练是关键，而推理时的想象是次要的**。实验显示，移除训练时的视频目标会导致性能大幅下降，而移除推理时的视频生成过程对成功率的影响微乎其微。
*   **数据效率**：即使没有大规模具身数据预训练，通过视频建模目标，模型也能在较小数据集上获得极强的泛化能力。

### 7. 优点
*   **高效性**：解决了 WAM 难以实时部署的痛点，使其具备了 VLA 级别的推理速度。
*   **科学性**：通过严谨的受控实验回答了一个基础性的学术问题，挑战了“WAM 必须在推理时进行想象”的固有认知。
*   **通用性**：在单臂、双臂、仿真及真实世界、刚体及柔性物体任务中均表现稳健。

### 8. 不足与局限
*   **预训练依赖**：虽然不依赖具身预训练，但 Fast-WAM 依然高度依赖强大的开源视频生成模型（如 Wan2.2）作为初始化权重。
*   **长程规划限制**：虽然在折叠毛巾等任务表现良好，但在需要极长时序闭环推理或复杂多步逻辑的任务中，显式的未来视频生成是否仍有不可替代的优势尚待进一步验证。
*   **算力信息缺失**：未详细披露训练所需的总计算资源消耗，不便于其他研究者评估复现成本。

（完）
