---
title: "vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models"
title_zh: vla-eval：视觉-语言-动作（VLA）模型的统一评估框架
authors: "Suhwan Choi, Yunsung Lee, Yubeen Park, Chris Dongjoo Kim, Ranjay Krishna, Dieter Fox, Youngjae Yu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13966v1"
tags: ["query:emb-bm"]
score: 9.0
evidence: 视觉-语言-动作模型的统一评估框架
tldr: 针对视觉-语言-动作（VLA）模型评估中存在的代码冗余、依赖冲突和协议不明确等问题，本文推出了开源评估框架 vla-eval。该框架通过 WebSocket 协议和 Docker 隔离技术解耦了模型推理与基准测试，支持 13 个基准测试和 6 种模型，并通过并行化实现了 47 倍的吞吐量提升，为 VLA 模型的标准化评估和复现提供了高效工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.13966v1/fig-001.webp\", \"caption\": \"Fig. 3. Wall-clock evaluation time: sequential vs. batch parallel. LIBERO: 2,000 episodes, 50 shards, B = 16. CALVIN: 1,000 sequences, 16 shards. SimplerEnv: 288 episodes (3 seeds), 16 shards.\", \"page\": 3, \"index\": 1, \"width\": 510, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2603.13966v1/fig-002.webp\", \"caption\": \"TABLE I SUPPORTED BENCHMARKS. DOCKER = IMAGE SIZE; ACT. = ACTION DIMENSIONALITY; STATUS: COMPLETE (REPRODUCTION VERIFIED), INTEGRATED (ADAPTER + DOCKER WORKING).\", \"page\": 2, \"index\": 2, \"width\": 519, \"height\": 664}, {\"url\": \"assets/figures/arxiv/2603.13966v1/fig-003.webp\", \"caption\": \"Fig. 4. VLA leaderboard (17 benchmarks, https://allenai.github.io/vla-evaluation-harness/leaderboard). Shown: models with >10 citations. Filterable by benchmark and model; contributions via pull request.\", \"page\": 4, \"index\": 3, \"width\": 914, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2603.13966v1/fig-004.webp\", \"caption\": \"Fig. 5. Distribution of benchmark coverage per model. 81% of the 509+ models in the leaderboard are evaluated on only one benchmark. Only 3 models (0.6%) are evaluated on 5 or more. Counts are lower bounds, as models may evaluate on benchmarks not tracked in the leaderboard. A unified evaluation framework is essential to make systematic cross-benchmark comparison feasible.\", \"page\": 4, \"index\": 4, \"width\": 444, \"height\": 281}]"
motivation: 现有的 VLA 模型评估缺乏统一标准，导致代码重复、环境依赖冲突且实验协议难以对齐。
method: 提出一种基于 WebSocket 通信和 Docker 隔离的解耦架构，通过标准化接口实现模型与基准测试的自动交叉评估。
result: 该框架实现了 47 倍的评估加速，在成功复现已发表模型性能的同时，揭示了影响结果的隐性文档要求和统计偏差。
conclusion: vla-eval 为 VLA 领域提供了标准化的评估基础设施和排行榜，显著提升了模型评估的效率、透明度与可复现性。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用由各模型仓库独立维护的特定基准测试脚本进行评估，这导致了代码重复、依赖冲突以及协议规范不明确等问题。我们提出了 vla-eval，这是一个开源评估框架，通过基于 Docker 的环境隔离和 WebSocket msgpack 协议，实现了模型推理与基准测试执行的解耦。模型只需实现单个 predict() 方法即可完成集成；基准测试通过四方法接口进行集成；完整的交叉评估矩阵可自动运行。完成一次完整的评估仅需两条命令：vla eval serve 和 vla eval run。该框架支持 13 个仿真基准测试和 6 个模型服务器。通过回合分片（episode sharding）和批处理推理实现的并行评估使吞吐量提升了 47 倍，在大约 18 分钟内即可完成 2000 个 LIBERO 回合。利用该基础设施，我们对一个已发布的 VLA 模型在三个基准测试上进行了复现性审计，发现这三个基准测试均能紧密复现已发布的数据，同时也揭示了一些未记录的需求、模糊的终止语义以及可能在无形中扭曲结果的隐藏归一化统计数据。此外，我们还发布了一个 VLA 排行榜，汇总了 17 个基准测试中的 657 项已发布结果。框架、评估配置及所有复现结果均已公开。

## 速览
**TLDR**：针对视觉-语言-动作（VLA）模型评估中存在的代码冗余、依赖冲突和协议不明确等问题，本文推出了开源评估框架 vla-eval。该框架通过 WebSocket 协议和 Docker 隔离技术解耦了模型推理与基准测试，支持 13 个基准测试和 6 种模型，并通过并行化实现了 47 倍的吞吐量提升，为 VLA 模型的标准化评估和复现提供了高效工具。 \
**Motivation**：现有的 VLA 模型评估缺乏统一标准，导致代码重复、环境冲突且实验协议难以对齐。 \
**Method**：提出一种基于 WebSocket 通信和 Docker 环境隔离的解耦架构，通过统一接口实现模型与基准测试的自动化交叉评估。 \
**Result**：框架实现了 47 倍的评估吞吐量提升，并成功复现了已发表模型的性能，同时揭示了影响结果的隐藏归一化统计等关键细节。 \
**Conclusion**：vla-eval 为 VLA 领域提供了标准化的评估基础设施和排行榜，显著提升了模型评估的效率与可复现性。

---

## Abstract
Vision Language Action VLA models are typically evaluated using per benchmark scripts maintained independently by each model repository, leading to duplicated code, dependency conflicts, and underspecified protocols. We present vla eval, an open source evaluation harness that decouples model inference from benchmark execution through a WebSocket msgpack protocol with Docker based environment isolation. Models integrate once by implementing a single predict() method; benchmarks integrate once via a four method interface; the full cross evaluation matrix works automatically. A complete evaluation requires only two commands: vla eval serve and vla eval run. The framework supports 13 simulation benchmarks and six model servers. Parallel evaluation via episode sharding and batch inference achieves a 47x throughput improvement, completing 2000 LIBERO episodes in about 18 minutes. Using this infrastructure, we conduct a reproducibility audit of a published VLA model across three benchmarks, finding that all three closely reproduce published values while uncovering undocumented requirements ambiguous termination semantics and hidden normalization statistics that can silently distort results. We additionally release a VLA leaderboard aggregating 657 published results across 17 benchmarks. Framework, evaluation configs, and all reproduction results are publicly available.

---

## 论文详细总结（自动生成）

### 论文总结：vla-eval —— 视觉-语言-动作（VLA）模型的统一评估框架

#### 1. 研究动机与核心问题
随着视觉-语言-动作（VLA）模型的快速发展，机器人操作领域的仿真基准测试日益增多。然而，现有的评估生态系统高度碎片化，存在四大核心痛点：
*   **重复劳动**：每个模型仓库都需独立维护多套基准测试脚本，且易与上游接口脱节。
*   **依赖与资产冲突**：不同基准测试（如 LIBERO、ManiSkill2、CALVIN）对 Python 版本和物理引擎（robosuite, SAPIEN, PyBullet）的要求互不兼容，难以在单一环境中运行。
*   **协议不明确**：论文常忽略随机种子、回合数、归一化统计数据等关键细节，导致复现困难。
*   **评估效率低下**：串行评估 2000 个回合通常需要十几个小时，限制了大规模消融实验的开展。

#### 2. 核心方法论
vla-eval 借鉴了语言模型评估框架（lm-evaluation-harness）的设计理念，核心思想是**解耦模型推理与基准测试执行**：
*   **客户端-服务器架构**：通过 WebSocket 协议和 msgpack 二进制序列化进行通信。模型作为服务器运行，基准测试作为客户端请求动作。
*   **Docker 隔离**：每个基准测试运行在独立的 Docker 容器中，解决了库依赖冲突和复杂的资产（场景、机器人描述文件）安装问题。
*   **标准化接口**：
    *   **模型端**：仅需实现一个 `predict(obs, ctx)` 方法（约 50 行代码），支持自动动作分块（Action Chunking）和集成策略。
    *   **基准测试端**：通过 `StepBenchmark` 接口实现 `reset`、`step`、`make_obs` 和 `get_step_result` 四个方法。
*   **并行化技术**：采用“回合分片（Episode Sharding）”实现环境并行，结合“批处理推理（Batch Inference）”提升模型端吞吐量。

#### 3. 实验设计与对比
*   **支持的基准测试**：集成了 13 个主流仿真环境，包括 LIBERO (及其变体)、CALVIN、SimplerEnv、ManiSkill2、RoboCasa、RLBench 等。
*   **支持的模型**：集成了 6 种先进 VLA 模型，如 OpenVLA、CogACT、π0、GR00T N1、X-VLA 等。
*   **复现性审计**：使用 DB-CogACT 模型在 LIBERO、CALVIN 和 SimplerEnv 三个基准测试上进行了严格的复现实验，对比已发表的原始分数。
*   **排行榜**：通过 AI 智能体（Claude Code）和人工审核，从 2685 篇论文中提取并标准化了 17 个基准测试下的 657 项结果。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA H100 GPU** 上进行。
*   **加速效果**：在 LIBERO 评估中，通过 50 个环境分片和 Batch Size=16 的配置，将 2000 个回合的评估时间从 **14 小时缩短至 18 分钟**（47 倍加速）。
*   **存储开销**：Docker 镜像大小从 4.7 GB 到 35.6 GB 不等（如 RoboCasa 较大）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 2000 个 LIBERO 回合、1000 个 CALVIN 链式序列和 288 个 SimplerEnv 回合的完整评估。
*   **充分性**：实验涵盖了从 6D 到 14D 的不同动作空间，验证了框架在不同复杂度环境下的通用性。
*   **客观性**：通过固定随机种子和版本化 Docker 镜像，确保了实验的公平性和可重复性。

#### 6. 主要结论与发现
*   **复现成功**：审计显示，在统一框架下，模型性能与已发表数值的偏差通常在 ±3% 以内。
*   **揭示隐性陷阱**：
    *   **语义模糊**：发现 SimplerEnv 的终止标志（terminated flag）仅代表瞬时成功，若提前停止会虚高得分，正确做法应运行至最大步数。
    *   **隐藏统计量**：CALVIN 需要特定的归一化均值和标准差，这些数据往往隐藏在训练代码中而非评估文档中。
*   **领域现状**：排行榜分析显示，81% 的模型仅在一个基准测试上评估，仅 0.6% 的模型评估超过 5 个基准，凸显了统一评估工具的必要性。

#### 7. 优点
*   **极简操作**：仅需两条命令（`serve` 和 `run`）即可启动复杂评估。
*   **高度解耦**：彻底解决了 VLA 领域长期存在的“环境配置地狱”问题。
*   **显著加速**：通过需求/供给匹配方法优化并行度，极大提升了科研效率。
*   **透明度**：自动保存完整的 Docker 标签、种子和配置，确保结果可追溯。

#### 8. 不足与局限
*   **覆盖范围**：目前仅涵盖仿真环境，**不支持真实机器人**评估。
*   **指标单一**：目前主要关注任务成功率，缺乏对动作质量、效率和安全性的多维度评估。
*   **审计深度**：复现性审计仅针对一个模型（DB-CogACT）进行，尚未开展更大规模的跨模型交叉验证。
*   **排行榜局限**：排行榜数据源于论文提取，而非全部通过该框架重新运行验证，可能存在原始论文本身的报告偏差。

（完）
