---
title: "DAM-VLA: A Dynamic Action Model-Based Vision-Language-Action Framework for Robot Manipulation"
title_zh: DAM-VLA：一种基于动态动作模型的机器人操纵视觉-语言-动作框架
authors: "Xiongfeng Peng, Jiaqian Yu, Dingzhe Li, Yixiang Jin, Lu Xu, Yamin Mao, Chao Zhang, Weiming Li, Sujin Jang, Dongwook Lee, Daehyun Ji"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.00926v1"
tags: ["query:1"]
score: 9.0
evidence: 基于动态动作模型的机器人操纵框架
tldr: DAM-VLA是一个针对机器人操作的视觉-语言-动作框架，旨在解决现有模型在通用任务适应性与精细操作精度之间难以平衡的问题。该框架通过集成预训练VLM的推理能力与基于扩散的专用动作模型，引入了动作路由、动态动作模型及双尺度动作加权机制。实验证明，DAM-VLA在模拟和真实环境的长程及接触密集型任务中表现优异，显著提升了操作成功率和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.00926v1/fig-001.webp\", \"caption\": \"Fig. 3: The architecture of our DAM-VLA. Given an RGB image observation and a task description, the model predicts a sequence of temporal actions. The process consists of three key components: 1) a vision-language model that encodes observation into visual, class and register tokens, and integrates visual tokens with a set of linguistic tokens, and produces the cognition and reasoning latents; 2) an action routing module that generates a weight and feeds it into the dynamic action model; 3) a dynamic action model that dynamically executes different action models by combining the low-level class token or register token from the vision model with the high-level cognition latent from the VLM to predict an action sequence.\", \"page\": 3, \"index\": 1, \"width\": 1000, \"height\": 511}]"
motivation: 现有的VLA框架在处理动态环境时，难以同时兼顾通用任务的理解能力和复杂精细操作所需的专业精度。
method: 该框架集成了VLM推理与扩散动作模型，通过动作路由、特征融合及双尺度动作加权机制实现机械臂与夹具的协同控制。
result: 在SIMPLER、FurnitureBench及真实世界的多项评估中，DAM-VLA在长程和接触密集型任务上的成功率均优于现有SOTA方法。
conclusion: 通过将高层认知推理与底层精细动作模型动态结合，DAM-VLA显著增强了机器人在复杂动态环境下的操作性能和泛化性。
---

## 摘要
在仓库、医院和家庭等动态环境中，机器人必须在粗略运动和精确操纵之间无缝切换，以完成复杂任务。然而，目前的视觉-语言-动作（VLA）框架大多改编自预训练的视觉-语言模型（VLM），往往难以在通用任务适应性与复杂操纵所需的专门精度之间取得平衡。为了应对这一挑战，我们提出了 DAM-VLA，一种基于动态动作模型的 VLA 框架。DAM-VLA 将 VLM 推理与专门用于机械臂和夹持器控制的基于扩散的动作模型相结合。具体而言，它引入了：(i) 一种动作路由机制，利用特定任务的视觉和语言线索来选择合适的动作模型（例如，机械臂移动或夹持器操纵）；(ii) 一种动态动作模型，融合了高层 VLM 认知与底层视觉特征以预测动作；以及 (iii) 一种双尺度动作加权机制，实现了机械臂移动模型与夹持器操纵模型之间的动态协调。在广泛的评估中，DAM-VLA 在模拟环境（SIMPLER、FurnitureBench）和真实场景中均取得了优于现有最先进 VLA 方法的成功率，展示了从标准取放任务到高要求的长时程及接触密集型任务的强大泛化能力。

## Abstract
In dynamic environments such as warehouses, hospitals, and homes, robots must seamlessly transition between gross motion and precise manipulations to complete complex tasks. However, current Vision-Language-Action (VLA) frameworks, largely adapted from pre-trained Vision-Language Models (VLMs), often struggle to reconcile general task adaptability with the specialized precision required for intricate manipulation. To address this challenge, we propose DAM-VLA, a dynamic action model-based VLA framework. DAM-VLA integrates VLM reasoning with diffusion-based action models specialized for arm and gripper control. Specifically, it introduces (i) an action routing mechanism, using task-specific visual and linguistic cues to select appropriate action models (e.g., arm movement or gripper manipulation), (ii) a dynamic action model that fuses high-level VLM cognition with low-level visual features to predict actions, and (iii) a dual-scale action weighting mechanism that enables dynamic coordination between the arm-movement and gripper-manipulation models. Across extensive evaluations, DAM-VLA achieves superior success rates compared to state-of-the-art VLA methods in simulated (SIMPLER, FurnitureBench) and real-world settings, showing robust generalization from standard pick-and-place to demanding long-horizon and contact-rich tasks.

---

## 论文详细总结（自动生成）

### DAM-VLA 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
在动态环境（如仓库、医院、家庭）中，机器人需要兼顾**大范围的粗略运动（Gross Motion）**和**局部的精细操纵（Precise Manipulation）**。现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然具备强大的通用任务理解和泛化能力，但在处理需要高精度的复杂操纵任务时往往表现不足。
论文指出，机械臂移动（Arm Movement）和夹持器操纵（Gripper Manipulation）在路径约束、视觉关注点及数据集分布上存在显著差异。因此，核心挑战在于如何将预训练视觉-语言模型（VLM）的高层推理能力与专门针对不同动作类型的精细控制模型动态结合。

#### 2. 方法论：核心思想与关键技术
DAM-VLA 提出了一种动态动作模型框架，其核心思想是**“分而治之”**：利用 VLM 进行任务路由，利用专门的扩散模型（Diffusion Models）执行具体动作。

*   **动作路由机制（Action Routing）：** 利用 VLM 生成的推理潜变量（Reasoning Latent），通过一个分类头预测权重 $w$。当 $w < 0.5$ 时执行机械臂移动模型，当 $w \geq 0.5$ 时执行夹持器操纵模型。
*   **动态动作模型（Dynamic Action Model）：** 包含两个基于扩散转换器（DiT）的专用头。
    *   **移动模型：** 结合 VLM 的认知特征与全局视觉特征（Class Token），负责大范围路径。
    *   **操纵模型：** 结合 VLM 特征与局部视觉特征（Register Token），负责精细抓取。
*   **双尺度动作加权（Dual-Scale Action Weighting）：**
    *   **轨迹级权重（Trajectory-level）：** 使用非对称高斯分布标记夹持器状态切换的关键时刻，强化对操纵阶段的监督。
    *   **动作块级权重（Action-chunk-level）：** 引入指数衰减因子，优先保证近期动作的准确性。
*   **损失函数：** 综合了分类损失（$L_{class}$）、移动模型扩散损失（$L_{move}$）和操纵模型扩散损失（$L_{mani}$）。

#### 3. 实验设计
*   **数据集：**
    *   **预训练：** 使用 Open X-Embodiment 数据集中的 Fractal 和 BridgeDataV2 子集。
    *   **微调：** 针对特定任务（如 FurnitureBench 的组装任务、真实世界的取放任务）进行微调。
*   **Benchmark 与场景：**
    *   **SIMPLER 仿真：** 包含 Google Robot 和 WidowX 两个平台，涵盖视觉匹配（VM）和变体聚合（VA）设置。
    *   **FurnitureBench：** 评估长时程（Long-horizon）和接触密集型（Contact-rich）任务（如拧桌腿）。
    *   **真实世界：** Franka 机器人的杯子取放任务，分为分布内（ID）和分布外（OOD）测试。
*   **对比方法：** RT-1, RT-2, OpenVLA, Octo, CogACT, $\pi_0$ 等 SOTA 模型。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA H100 GPU**。
*   **训练时长：** 预训练阶段大约持续 **2 天**。
*   **超参数：** 学习率为 $2 \times 10^{-5}$，Batch Size 为 256。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个仿真平台（SIMPLER, FurnitureBench）和真实世界中进行了广泛测试。在 SIMPLER 中涵盖了 8 个子任务，在真实世界中进行了 80 次试验（16 种工况）。
*   **消融实验：** 针对视觉 Token、动作块权重、轨迹权重、动态动作模型及双潜变量（Reasoning/Cognition Latents）进行了详细的消融分析。
*   **客观性：** 实验对比了多种主流 VLA 模型，并使用了标准化的成功率作为指标，且在分布外（OOD）场景下验证了泛化性，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **性能领先：** DAM-VLA 在 SIMPLER 仿真中的平均成功率显著高于现有方法（如在 Google Robot VM 设置下达到 83%）。
*   **复杂任务优势：** 在长时程和接触密集的 FurnitureBench 任务中，DAM-VLA 在“拧桌腿”等关键步骤上的表现远超 OpenVLA 和 CogACT。
*   **鲁棒性：** 动态路由机制使得模型在面对背景变化、遮挡和新物体（OOD 场景）时具有更强的稳定性。
*   **特征融合：** 证明了将 VLM 的高层语义与 DINOv2/SigLIP 的底层视觉特征结合对动作预测至关重要。

#### 7. 优点（亮点）
*   **架构创新：** 巧妙地解决了 VLM 推理速度慢与机器人控制频率要求高之间的矛盾，通过路由机制实现了专用模型的动态调用。
*   **视觉特征精细化：** 区分了全局 Class Token 和局部 Register Token 的用途，符合机器人操作的直觉。
*   **加权机制科学：** 双尺度加权机制有效解决了机器人数据集中“移动多、操纵少”的数据不平衡问题，提升了关键动作的精度。

#### 8. 不足与局限
*   **任务覆盖面：** 虽然涉及了接触密集型任务，但主要仍集中在取放和简单组装，尚未扩展到更复杂的双臂协同或柔性物体操纵。
*   **模型扩展性：** 目前仅在两个专用动作模型（移动 vs 操纵）之间路由，若未来需要处理更多种类的动作（如擦拭、切割等），路由机制的复杂度和训练难度会增加。
*   **微小提升：** 在某些特定任务（如“绿块叠黄块”）上，相比于基座模型的提升幅度相对较小，说明在极精细的空间坐标对齐上仍有优化空间。

（完）
