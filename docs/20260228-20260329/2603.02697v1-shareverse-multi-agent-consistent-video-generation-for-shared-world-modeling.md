---
title: "ShareVerse: Multi-Agent Consistent Video Generation for Shared World Modeling"
title_zh: ShareVerse：面向共享世界建模的多智能体一致性视频生成
authors: "Jiayi Zhu, Jianing Zhang, Yiying Yang, Wei Cheng, Xiaoyun Yuan"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02697v1"
tags: ["query:emb-bm"]
score: 9.0
evidence: 基于视频生成的多智能体交互式世界建模数据集与框架
tldr: ShareVerse 是一个多智能体一致性视频生成框架，旨在解决现有模型在构建统一共享世界和多智能体交互方面的不足。该研究基于 CARLA 平台构建了大规模多智能体交互数据集，并提出了四视图空间拼接策略和跨智能体注意力机制。通过这些创新，ShareVerse 能够生成长达 49 帧的高质量视频，在确保重叠区域一致性的同时，实现了对动态智能体位置的精准感知和共享世界的连贯建模。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.02697v1/fig-001.webp\", \"caption\": \"Figure 3: Qualitative results. a) The four-view video of a certain agent maintains strong internal consistency. b) Agents generate visual scenes based on camera trajectories and interact with dynamically generated information, rather than being conditioned solely on the first frame. c) We compare the generated videos before and after the removal of certain buildings within a map. The sample shows that agents effectively exchange information based on the scene context, achieving a shared world.\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 994}, {\"url\": \"assets/figures/arxiv/2603.02697v1/fig-002.webp\", \"caption\": \"Figure 2: Method overview. Given one image from each of two agents, where four views are concatenated to depict a scene, ShareVerse performs a prediction task of 49 video frames to generate future videos conditioned on the camera trajectories from users. In the generation process, two agents explore the world, exchange captured visual information, and perceive each other’s positions.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2603.02697v1/fig-003.webp\", \"caption\": \"Figure 4: Dynamic positions. We keep the trajectory of one agent fixed and modify the other to generate two video samples. Comparison with the ground truth demonstrates that our model can accurately perceive the positions of the other agent.\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2603.02697v1/fig-004.webp\", \"caption\": \"Figure 1: Illustration of the dataset construction process. We build the multi-agent synchronized training dataset by rendering in CARLA. We equip each agent with four cameras (front, rear, left, and right) and predefine six main trajectory pairs, as shown in (a) – (f). Each pair enables interaction between agents.\", \"page\": 3, \"index\": 4, \"width\": 826, \"height\": 358}]"
motivation: 现有视频生成模型在处理多智能体交互和构建统一的共享世界建模方面存在明显局限。
method: 提出了基于 CARLA 的多视图数据集、四视图空间拼接策略以及用于跨智能体时空信息交换的注意力机制。
result: 该框架支持生成 49 帧的大规模视频，并能精准感知动态智能体位置，确保了共享世界在重叠与非重叠区域的一致性。
conclusion: ShareVerse 有效实现了多智能体协同下的共享世界建模，为复杂交互场景的视频生成提供了新方案。
---

## 摘要
本文提出了 ShareVerse，这是一个支持多智能体共享世界建模的视频生成框架，填补了现有研究在支持多智能体交互的统一共享世界构建方面的空白。ShareVerse 利用了大型视频模型的生成能力，并集成了三项关键创新：1) 在 CARLA 仿真平台上构建了一个用于大规模多智能体交互式世界建模的数据集，包含多样的场景、天气条件和交互轨迹，并配有成对的多视图视频（每个智能体的前/后/左/右视图）和相机数据。2) 我们为独立智能体的四视图视频提出了一种空间拼接策略，以建模更广泛的环境并确保内部多视图的几何一致性。3) 我们在预训练的视频模型中集成了跨智能体注意力模块，实现了智能体之间时空信息的交互传输，从而保证了重叠区域的共享世界一致性以及非重叠区域的合理生成。ShareVerse 支持 49 帧的大规模视频生成，能够准确感知动态智能体的位置，并实现了一致的共享世界建模。

## 速览
**TLDR**：ShareVerse 是一个多智能体一致性视频生成框架，旨在解决现有模型在构建统一共享世界和多智能体交互方面的不足。该研究基于 CARLA 平台构建了大规模多智能体交互数据集，并提出了四视图空间拼接策略和跨智能体注意力机制。通过这些创新，ShareVerse 能够生成长达 49 帧的高质量视频，在确保重叠区域一致性的同时，实现了对动态智能体位置的精准感知和共享世界的连贯建模。 \
**Motivation**：现有视频生成模型在处理多智能体交互和构建统一的共享世界建模方面存在明显局限。 \
**Method**：提出了基于 CARLA 的多视图数据集、四视图空间拼接策略以及用于跨智能体时空信息交换的注意力机制。 \
**Result**：该框架支持生成 49 帧的大规模视频，并能精准感知动态智能体位置，确保了共享世界在重叠与非重叠区域的一致性。 \
**Conclusion**：ShareVerse 有效实现了多智能体协同下的共享世界建模，为复杂交互场景的视频生成提供了新方案。

---

## Abstract
This paper presents ShareVerse, a video generation framework enabling multi-agent shared world modeling, addressing the gap in existing works that lack support for unified shared world construction with multi-agent interaction. ShareVerse leverages the generation capability of large video models and integrates three key innovations: 1) A dataset for large-scale multi-agent interactive world modeling is built on the CARLA simulation platform, featuring diverse scenes, weather conditions, and interactive trajectories with paired multi-view videos (front/ rear/ left/ right views per agent) and camera data. 2) We propose a spatial concatenation strategy for four-view videos of independent agents to model a broader environment and to ensure internal multi-view geometric consistency. 3) We integrate cross-agent attention blocks into the pretrained video model, which enable interactive transmission of spatial-temporal information across agents, guaranteeing shared world consistency in overlapping regions and reasonable generation in non-overlapping regions. ShareVerse, which supports 49-frame large-scale video generation, accurately perceives the position of dynamic agents and achieves consistent shared world modeling.

---

## 论文详细总结（自动生成）

以下是对论文《ShareVerse: Multi-Agent Consistent Video Generation for Shared World Modeling》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的视频生成模型（如 Sora、CogVideoX 等）虽然在单视角或物体级多视角生成上取得了突破，但仍无法支持**多智能体在统一共享世界中的交互建模**。
*   **研究背景**：在多人游戏、多机器人协作和无人机群等应用中，需要多个独立智能体在同一个物理世界中行动，且各自生成的视频视角必须在时空上保持全局一致性（即“共享世界”）。目前的模型往往局限于单视角或缺乏跨智能体的动态交互感知。

### 2. 方法论
ShareVerse 基于预训练的视频生成模型 CogVideoX，提出了以下核心技术：
*   **空间拼接策略（Spatial Concatenation）**：将单个智能体的四个视角（前、后、左、右）在空间上拼接成一个统一的视频流。这种设计能覆盖 360° 视野，并利用模型内在的上下文生成能力确保单智能体内部的多视角几何一致性。
*   **相机轨迹处理（Raymap Encoding）**：将相机的内参和位姿转换为 6 通道的 Raymap（包含射线方向和起点）。通过 Raymap 编码器将其嵌入到 Diffusion Transformer (DiT) 的特征中，实现对智能体运动轨迹的精确控制。
*   **跨智能体注意力模块（Cross-Agent Attention Blocks）**：
    *   这是实现“共享”的关键。该模块将两个智能体的视频特征沿帧维度拼接，通过注意力机制交换位置和视觉信息。
    *   引入旋转位置嵌入（RoPE）处理双倍长度的序列。
    *   确保了智能体在视野重叠区域生成相同的内容，在非重叠区域基于历史信息进行合理推断。

### 3. 实验设计
*   **数据集**：由于缺乏现成的多智能体同步视频数据，研究者基于 **CARLA 仿真平台**构建了一个大规模数据集。
    *   包含 55,000 对同步视频，涵盖 10 多种场景、3 种天气。
    *   设计了 6 种典型的交互轨迹（如十字路口会车、转弯相遇等）。
    *   每个智能体配备 4 个同步摄像头，视频长度为 49 帧。
*   **Benchmark 与对比**：
    *   **图像重建指标**：PSNR、SSIM、LPIPS（用于评估世界一致性）。
    *   **视频质量指标**：使用 VBench 评估美学、成像、时间抖动、运动平滑度、主体/背景一致性。
    *   **消融实验**：对比了单视角 vs 四视角拼接、Raymap vs 原始数值输入、以及是否有跨智能体注意力模块的效果。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   基于 CogVideoX-5B-I2V 架构。
    *   训练步数为 10,000 步。
    *   学习率为 $2 \times 10^{-5}$，Batch Size 为 16。
    *   视频分辨率为 480 × 720，长度为 49 帧。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了定量评估（表 1、表 2）、定性视觉展示（图 3、图 4）以及多项消融实验。
*   **充分性评价**：实验设计较为充分。通过在未见过的场景上进行验证，证明了模型的泛化能力。消融实验清晰地展示了四视角拼接和跨智能体注意力机制对维持世界一致性的必要性。实验对比客观，既展示了高分指标，也诚实地讨论了 PSNR 指标受生成多样性影响的现象。

### 6. 主要结论与发现
*   **共享一致性**：ShareVerse 成功实现了多智能体在复杂交互场景下的全局时空一致性，两个智能体看到的“世界”是统一的。
*   **位置感知**：模型能够准确感知对方智能体的动态位置，并在视频中正确渲染出对方的车辆（如红色的 Audi TT）。
*   **多视角优势**：相比单视角训练，四视角拼接训练能显著提升模型对环境的理解，因为更广的视野有助于智能体之间交换更完整的视觉信息。

### 7. 优点与亮点
*   **开创性任务**：率先提出了多智能体共享世界视频生成这一新研究方向。
*   **高质量数据集**：填补了多智能体同步交互视频数据的空白，且数据生成流程具有可扩展性。
*   **架构优雅**：通过简单的特征拼接和跨智能体注意力机制，在不破坏预训练模型生成能力的前提下，实现了复杂的一致性约束。

### 8. 不足与局限
*   **数据偏差**：目前完全依赖 CARLA 仿真数据，与真实物理世界的视觉分布仍有差距，可能存在 Sim-to-Real 的泛化风险。
*   **智能体数量限制**：实验主要集中在两个智能体的交互，尚未验证在更多智能体（如 10 个以上）同时存在时的计算开销和一致性表现。
*   **指标局限**：PSNR 等传统指标在衡量具有高度随机性的生成式模型时存在局限，无法完全反映“物理合理性”。
*   **实时性**：虽然支持 49 帧生成，但论文未详细讨论推理延迟，对于实时交互应用可能仍有挑战。

（完）
