---
title: "Fast-WAM: Do World Action Models Need Test-time Future Imagination?"
title_zh: Fast-WAM：世界动作模型在测试时需要未来想象吗？
authors: "Tianyuan Yuan, Zibin Dong, Yicheng Liu, Hang Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16666v1"
tags: ["query:1"]
score: 10.0
evidence: 用于具身控制和视频建模的世界动作模型 (WAM)
tldr: 本研究探讨了世界动作模型（WAM）在推理阶段是否需要显式的未来想象。针对现有模型因迭代视频去噪导致的高延迟问题，本文提出 Fast-WAM，在训练时保留视频协同训练以增强表征能力，但在推理时跳过未来预测直接输出动作。实验证明，Fast-WAM 在多个基准测试中表现优异，且推理速度提升 4 倍以上，揭示了视频预测的主要价值在于训练阶段的表征学习而非推理时的生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.16666v1/fig-001.webp\", \"caption\": \"Figure 1: Three representative WAM paradigms. (A) Joint-modeling WAMs denoise future video and action tokens together. (B) Causal WAMs first generate future observations and then condition action prediction on the generated future representation. (C) Fast-WAM retains video co-training during training but removes explicit future generation at inference time, directly predicting actions from latent world representations in a single forward pass.\", \"page\": 2, \"index\": 1, \"width\": 824, \"height\": 457}, {\"url\": \"assets/figures/arxiv/2603.16666v1/fig-002.webp\", \"caption\": \"Table 1: Results on RoboTwin. Fast-WAM matches strong pretrained WAM baselines without using embodied pretraining, while the two imagine-then-execute variants remain highly comparable and removing video co-training causes a substantial drop.\", \"page\": 7, \"index\": 2, \"width\": 656, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2603.16666v1/fig-003.webp\", \"caption\": \"Figure 3: Real-world towel-folding task on the Galaxea R1 Lite platform. Folding a deformable object requires long-horizon planning and precise closed-loop manipulation, making it a challenging benchmark for evaluating both task success and execution efficiency.\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.16666v1/fig-004.webp\", \"caption\": \"Table 3: Per-task success rates on RoboTwin under clean and randomized evaluation settings.\", \"page\": 13, \"index\": 4, \"width\": 822, \"height\": 932}, {\"url\": \"assets/figures/arxiv/2603.16666v1/fig-005.webp\", \"caption\": \"Table 2: Results on LIBERO. Fast-WAM achieves competitive overall performance without embodied pretraining, remains close to both imagine-then-execute variants, and outperforms the no-video-cotraining ablation by a clear margin.\", \"page\": 8, \"index\": 5, \"width\": 776, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2603.16666v1/fig-006.webp\", \"caption\": \"Figure 4: Real-world results on the long-horizon towel-folding task. The left panel plots success rate against average completion time, where upper-left is better. The right panel compares inference latency. Fast-WAM achieves strong real-world performance with substantially lower latency than imagine-then-execute variants, while removing video co-training degrades both success rate and completion time.\", \"page\": 9, \"index\": 6, \"width\": 771, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2603.16666v1/fig-007.webp\", \"caption\": \"Figure 2: Fast-WAM architecture and the structured attention mask used to disentangle video cotraining from action generation.\", \"page\": 5, \"index\": 7, \"width\": 715, \"height\": 413}]"
motivation: 现有世界动作模型依赖推理时的视频生成导致高延迟，且不确定显式未来想象对动作性能是否具有必要性。
method: 提出 Fast-WAM 架构，在训练中结合视频建模任务来增强表征，但在推理阶段直接输出动作而无需生成未来图像。
result: Fast-WAM 在仿真和真实任务中达到领先水平，推理延迟仅为 190ms，比传统“先想象后执行”模型快 4 倍以上。
conclusion: 视频预测在 WAM 中的核心价值在于训练期间提升世界表征能力，而非推理时的显式未来观测生成。
---

## 摘要
世界动作模型（WAMs）已成为具身控制中视觉-语言-动作（VLA）模型的一种极具前景的替代方案，因为它们显式地建模了视觉观测在动作作用下如何演化。大多数现有的 WAM 遵循“先想象后执行”的范式，由于迭代视频去噪产生了显著的测试时延迟，但目前尚不清楚显式的未来想象对于强大的动作性能是否真的必要。在本文中，我们探讨了 WAM 在测试时是否需要显式的未来想象，或者其收益是否主要源于训练期间的视频建模。我们通过提出 Fast-WAM 来解耦训练期间视频建模的作用与推理期间显式未来生成的作用。Fast-WAM 是一种在训练时保留视频协同训练但在测试时跳过未来预测的 WAM 架构。我们进一步实例化了多个 Fast-WAM 变体，以便对这两个因素进行受控比较。在这些变体中，我们发现 Fast-WAM 与“先想象后执行”变体相比仍具有竞争力，而移除视频协同训练会导致更大幅度的性能下降。实验表明，Fast-WAM 在模拟基准测试（LIBERO 和 RoboTwin）以及现实世界任务中均取得了与最先进方法相当的结果，且无需具身预训练。它的运行延迟为 190 毫秒，实现了实时运行，比现有的“先想象后执行”型 WAM 快 4 倍以上。这些结果表明，WAM 中视频预测的主要价值可能在于改进训练期间的世界表示，而不是在测试时生成未来观测。项目主页：https://yuantianyuan01.github.io/FastWAM/

## 速览
**TLDR**：本研究探讨了世界动作模型（WAM）在推理时是否需要显式的未来想象。作者提出了Fast-WAM，该模型在训练阶段保留视频协同训练以学习强大的世界表示，但在推理时跳过耗时的未来预测步骤。实验表明，Fast-WAM在LIBERO和RoboTwin等基准测试中表现优异，且推理速度比传统“先想象后执行”模型快4倍以上，证明了视频预测的主要价值在于提升训练时的表示能力而非测试时的显式生成。 \
**Motivation**：现有的世界动作模型因推理时的迭代视频去噪导致高延迟，需探究显式的未来想象在测试阶段是否必不可少。 \
**Method**：提出Fast-WAM架构，通过在训练中保留视频建模但在推理时移除未来预测步骤，实现高效的动作生成。 \
**Result**：Fast-WAM在仿真和真实任务中达到了领先的性能，且延迟仅为190ms，比现有模型快4倍以上。 \
**Conclusion**：视频预测在世界动作模型中的核心价值在于训练期间优化世界表示，而非测试时的显式未来生成。

---

## Abstract
World Action Models (WAMs) have emerged as a promising alternative to Vision-Language-Action (VLA) models for embodied control because they explicitly model how visual observations may evolve under action. Most existing WAMs follow an imagine-then-execute paradigm, incurring substantial test-time latency from iterative video denoising, yet it remains unclear whether explicit future imagination is actually necessary for strong action performance. In this paper, we ask whether WAMs need explicit future imagination at test time, or whether their benefit comes primarily from video modeling during training. We disentangle the role of video modeling during training from explicit future generation during inference by proposing \textbf{Fast-WAM}, a WAM architecture that retains video co-training during training but skips future prediction at test time. We further instantiate several Fast-WAM variants to enable a controlled comparison of these two factors. Across these variants, we find that Fast-WAM remains competitive with imagine-then-execute variants, while removing video co-training causes a much larger performance drop. Empirically, Fast-WAM achieves competitive results with state-of-the-art methods both on simulation benchmarks (LIBERO and RoboTwin) and real-world tasks, without embodied pretraining. It runs in real time with 190ms latency, over 4$\times$ faster than existing imagine-then-execute WAMs. These results suggest that the main value of video prediction in WAMs may lie in improving world representations during training rather than generating future observations at test time. Project page: https://yuantianyuan01.github.io/FastWAM/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-WAM: Do World Action Models Need Test-time Future Imagination?》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：世界动作模型（World Action Models, WAMs）通过建模未来视觉观测来辅助动作预测，在具身智能领域表现优异。然而，现有的 WAM 普遍采用“先想象后执行”的范式，即在推理时通过迭代去噪生成未来视频帧，这导致了极高的计算延迟。
*   **核心问题**：论文提出了一个根本性的疑问：**WAM 的性能提升究竟源于推理时的“显式未来想象”，还是源于训练阶段“视频建模任务”带来的更强表征能力？** 如果是后者，那么推理时的视频生成过程或许可以被省略，从而大幅提升实时性。

### 2. 论文提出的方法论：Fast-WAM
*   **核心思想**：解耦训练时的视频协同训练（Video Co-training）与推理时的未来生成。在训练时利用视频预测任务来塑造物理感知的世界表征，但在推理时跳过视频去噪，直接从潜空间表征中预测动作。
*   **关键技术细节**：
    *   **架构设计**：基于 Wan2.2-5B 视频扩散 Transformer（DiT）。采用“混合 Transformer（MoT）”架构，包含一个共享权重的视频 DiT 分支和一个专门的动作专家 DiT 分支（1B 参数）。
    *   **结构化注意力掩码**：在训练中，通过掩码确保动作分支只能访问当前帧的清洁潜变量，而不能“偷看”未来的视频预测结果。这保证了推理时即使没有未来视频输入，动作分支依然能正常工作。
    *   **训练目标**：采用联合流匹配（Flow Matching）目标函数：
        $$L = L_{act} + \lambda L_{vid}$$
        其中 $L_{act}$ 负责动作块生成，$L_{vid}$ 负责未来视频帧的潜变量预测。
    *   **推理流程**：在测试阶段，模型仅对当前观测帧进行单次前向传播，提取潜空间特征后直接交给动作专家生成动作序列，完全避开了耗时的视频去噪循环。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务，评估长程规划和空间推理能力。
    *   **RoboTwin 2.0**：具有挑战性的双臂协同操作基准，包含 50 多个任务，涉及重度场景随机化。
    *   **真实世界任务**：折叠毛巾（Towel Folding）。这是一个涉及柔性物体动力学的长程任务，对闭环控制和物理推理要求极高。
*   **对比方法**：
    *   **VLA 基准**：OpenVLA, $\pi_0$, $\pi_{0.5}$。
    *   **WAM 基准**：Motus, LingBot-VA。
    *   **受控变体（消融）**：
        1.  **Fast-WAM-Joint**：推理时联合去噪视频和动作。
        2.  **Fast-WAM-IDM**：先生成视频再预测动作（传统 WAM 模式）。
        3.  **Fast-WAM w.o. video co-train**：移除训练时的视频建模任务。

### 4. 资源与算力
*   **硬件环境**：推理延迟在单张 **NVIDIA RTX 5090D V2 (32GB)** GPU 上测量。
*   **模型规模**：总参数量约为 **6B**（5B 视频主干 + 1B 动作专家）。
*   **训练时长**：LIBERO 训练 20k 步，RoboTwin 和真实世界任务训练 30k 步。文中未明确提及总训练小时数，但指出其具有较高的数据效率，无需大规模具身数据预训练。

### 5. 实验数量与充分性
*   **实验规模**：在仿真环境中进行了数千次试验（LIBERO 2000 次，RoboTwin 每任务 100 次），并在真实世界中进行了详尽的成功率与完成时间评估。
*   **充分性与公平性**：实验设计非常严谨。通过在相同的代码框架和主干网络下实现“联合去噪”、“因果生成”和“无视频训练”等变体，排除了网络架构差异的影响，直接孤立了“推理时想象”这一变量。这种受控实验（Controlled Comparison）极具说服力。

### 6. 主要结论与发现
*   **推理想象非必要**：Fast-WAM 在不生成未来视频的情况下，性能与显式生成的变体（Joint/IDM）高度接近。
*   **训练建模是核心**：移除训练时的视频协同任务会导致性能大幅下降（如在 RoboTwin 上从 91.8% 降至 83.8%），证明 WAM 的价值在于训练时的表征学习。
*   **高效实时性**：Fast-WAM 的推理延迟仅为 **190ms**，比传统的“先想象后执行”模型快 **4 倍以上**，能够满足实时控制需求。
*   **数据效率**：即使没有具身预训练，Fast-WAM 也能在小规模演示数据上达到甚至超过经过大规模预训练的 VLA 模型。

### 7. 优点
*   **范式突破**：挑战了 WAM 必须在推理时“想象”的固有认知，为高效具身智能提供了新思路。
*   **实用性强**：显著降低了部署 WAM 的算力门槛和延迟，使其更适合真实机器人应用。
*   **实验严谨**：通过解耦实验清晰地回答了科学假设，结论具有启发性。

### 8. 不足与局限
*   **预训练规模限制**：实验主要基于 Wan2.2 视频模型进行微调，尚未探讨在更大规模（如百亿级参数）或更广博的具身预训练数据下，该结论是否依然成立。
*   **任务复杂度**：虽然涵盖了柔性物体，但对于需要极高精度预测（如毫秒级物理反馈）的极端动态任务，显式未来想象是否会展现出不可替代的优势仍有待验证。
*   **自回归限制**：为了公平对比，实验省略了外层的自回归循环，这可能掩盖了长时程预测在某些极端长任务中的潜在收益。

（完）
