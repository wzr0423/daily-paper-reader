---
title: Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning
title_zh: 结合可微世界模型的离线强化学习模型预测控制
authors: "Rohan Deb, Stephen J. Wright, Arindam Banerjee"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22430v1"
tags: ["query:1"]
score: 9.0
evidence: 结合学习的状态转移和奖励世界模型，用于离线强化学习的模型预测控制
tldr: 本研究针对离线强化学习中策略在推理阶段缺乏灵活性且无法进一步优化的局限，提出了一种结合可微世界模型（DWM）的模型预测控制（MPC）框架。该方法通过在推理时对想象的轨迹进行端到端梯度计算，实现了策略参数的动态在线优化。在D4RL基准测试上的实验证明，这种推理时自适应机制能显著提升现有离线RL算法的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22430v1/fig-001.webp\", \"caption\": \"Figure 1. Inference-time MPC with a diffusion world model. An offline dataset is used to train (i) a policy πψ and terminal critic Qϕ (ii) a reward model rξ , and (iii) a diffusion-based dynamics sampler fθ . At inference time, starting from the current state st, we unroll multiple imagined rollouts by alternating policy actions ãh = πψ(s̃h) and diffusion transitions s̃h+1 = fθ(s̃h, ãh, εt+h), evaluate a finite-horizon surrogate return (predicted rewards plus terminal value), and backpropagate through the differentiable rollout to update ψ before executing the first action in the real environment. Green arrows indicate the forward rollout; red dashed arrows indicate gradient flow.\", \"page\": 2, \"index\": 1, \"width\": 1005, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2603.22430v1/fig-002.webp\", \"caption\": \"Figure 2. One-step state prediction RMSE of diffusion models across training steps (20k–200k) on medium-replay datasets. RMSE decreases with training for all three environments (halfcheetah, hopper, walker2d), with shaded regions showing standard error over 1000 transitions.\", \"page\": 7, \"index\": 2, \"width\": 1002, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.22430v1/fig-003.webp\", \"caption\": \"Figure 3. Reward prediction RMSE of reward models across training steps (20k–200k) on medium-replay datasets. All environments show decreasing RMSE with training. Shaded regions indicate standard error over 1000 transitions.\", \"page\": 7, \"index\": 3, \"width\": 1002, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.22430v1/fig-004.webp\", \"caption\": \"Table 1. Average normalized score over the final evaluation and ten unseen training seeds on Gym-MuJoCo tasks. TD3+BC IQL CQL and SAC-RND scores are taken from (Tarasov et al., 2023a). We ran ReBRAC and MPCwDWM with ReBRAC as the pre-trained policy. The symbol ± represents the standard deviation across the seeds. (⋆) We did not see an improvement with inference-time optimization and the performance is that of the pre-trained ReBRAC policy.\", \"page\": 8, \"index\": 4, \"width\": 1010, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.22430v1/fig-005.webp\", \"caption\": \"Table 2. Normalized scores on D4RL MuJoCo tasks. DT, TT, MOPO, MOReL, MBOP, and Diffuser values are taken from (Janner et al., 2022b). The symbol ± represents the standard deviation across the seeds.\", \"page\": 9, \"index\": 5, \"width\": 912, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2603.22430v1/fig-006.webp\", \"caption\": \"Table 4. One step RMSE (mean ± standard error) on D4RL MuJoCo datasets.\", \"page\": 16, \"index\": 6, \"width\": 565, \"height\": 556}, {\"url\": \"assets/figures/arxiv/2603.22430v1/fig-007.webp\", \"caption\": \"Table 5. Reward prediction RMSE (mean ± standard error) on D4RL MuJoCo datasets.\", \"page\": 16, \"index\": 7, \"width\": 554, \"height\": 556}]"
motivation: 传统的离线强化学习方法在推理阶段仅应用预训练策略，缺乏根据实时环境反馈进行动态调整和优化的能力。
method: 构建了一个可微世界模型流水线，利用模型预测控制（MPC）在推理阶段通过想象轨迹的端到端梯度来实时优化策略参数。
result: 在D4RL的MuJoCo运动任务和AntMaze等连续控制基准测试中，该方法一致性地优于多个强力离线强化学习基准模型。
conclusion: 通过可微世界模型在推理阶段利用实时信息优化策略，是增强离线强化学习模型泛化与执行能力的有效手段。
---

## 摘要
离线强化学习（Offline RL）旨在从固定的离线数据集中学习最优策略，而无需与环境进行进一步交互。此类方法通常训练一个离线策略（或价值函数），并在推理时直接应用而不再进行精细化调整。我们提出了一种受模型预测控制（MPC）启发的推理阶段自适应框架，该框架利用预训练策略以及学习到的状态转移和奖励世界模型。虽然现有的世界模型和扩散规划方法利用学习到的动力学在训练期间生成想象轨迹，或在推理阶段采样候选规划，但它们并未利用推理阶段的信息来实时优化策略参数。相比之下，我们的设计是一个可微世界模型（DWM）流水线，能够通过想象展开（imagined rollouts）进行端到端梯度计算，从而在推理阶段基于 MPC 优化策略参数。我们在 D4RL 连续控制基准测试（MuJoCo 运动任务和 AntMaze）上评估了我们的算法，结果表明，利用推理阶段信息来优化策略参数，相比强大的离线强化学习基准模型取得了持续的性能提升。

## Abstract
Offline Reinforcement Learning (RL) aims to learn optimal policies from fixed offline datasets, without further interactions with the environment. Such methods train an offline policy (or value function), and apply it at inference time without further refinement. We introduce an inference time adaptation framework inspired by model predictive control (MPC) that utilizes a pretrained policy along with a learned world model of state transitions and rewards. While existing world model and diffusion-planning methods use learned dynamics to generate imagined trajectories during training, or to sample candidate plans at inference time, they do not use inference-time information to optimize the policy parameters on the fly. In contrast, our design is a Differentiable World Model (DWM) pipeline that enables endto-end gradient computation through imagined rollouts for policy optimization at inference time based on MPC. We evaluate our algorithm on D4RL continuous-control benchmarks (MuJoCo locomotion tasks and AntMaze), and show that exploiting inference-time information to optimize the policy parameters yields consistent gains over strong offline RL baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的离线强化学习（Offline RL）通常遵循“先训练、后部署”的模式，在推理阶段策略参数是固定的。这种方式完全依赖于预训练阶段对 Q 函数或策略的准确估计，但在面对分布偏移或复杂长程任务时，预训练模型往往难以提供最优决策。
*   **研究动机**：借鉴大语言模型（LLM）在推理时增加计算量（如思维链、搜索）能提升性能的思路，作者提出：在离线 RL 的推理阶段，是否也可以通过额外的计算来实时优化策略？
*   **整体含义**：本文提出了一种结合**可微世界模型（DWM）**与**模型预测控制（MPC）**的框架，允许智能体在推理时通过“想象”未来的轨迹，并利用梯度下降实时调整策略参数，从而弥补离线预训练的不足。

### 2. 方法论：MPCwDWM 框架
*   **核心思想**：在推理阶段，智能体不直接执行预训练策略的动作，而是先在学习到的“世界模型”中进行多步模拟（Rollout），计算模拟轨迹的累积奖励，并通过反向传播更新策略参数，最后执行优化后的动作。
*   **关键技术细节**：
    1.  **可微扩散世界模型**：使用条件扩散模型（Conditional Diffusion Model）学习状态转移 $P(s'|s, a)$。通过将扩散过程表示为确定性计算图，使得生成的样本对输入状态和动作是可微的。
    2.  **奖励与价值模型**：训练一个可微的奖励模型 $r(s, a)$ 和一个终端价值函数 $Q(s, a)$（由预训练的离线 RL 算法提供）。
    3.  **推理时优化**：在每个真实环境步 $t$，从当前状态 $s_t$ 出发，利用扩散模型生成 $M$ 条长度为 $H$ 的想象轨迹。
    4.  **梯度递归**：通过链式法则计算目标函数（$H$ 步奖励总和 + 终端 $Q$ 值）关于策略参数 $\psi$ 的梯度。论文给出了扩散模型采样过程中的雅可比矩阵递归公式（Theorem 4.1），实现了端到端的参数更新。
*   **算法流程**：观察状态 $\rightarrow$ 想象展开 $\rightarrow$ 计算模拟回报 $\rightarrow$ 梯度回传更新策略 $\psi$ $\rightarrow$ 执行更新后的策略动作 $\rightarrow$ 进入下一真实状态。

### 3. 实验设计
*   **数据集/场景**：使用了标准的 **D4RL** 连续控制基准测试。
    *   **Gym-MuJoCo 运动任务**：包含 HalfCheetah, Hopper, Walker2d 的 18 个子任务（Random, Medium, Expert 等不同数据质量）。
    *   **AntMaze 迷宫任务**：包含 6 个更具挑战性的任务（UMaze, Medium, Large），侧重于长程规划。
*   **对比基准（Baselines）**：
    *   **传统离线 RL**：TD3+BC, IQL, CQL, SAC-RND, 以及作为基础策略的 ReBRAC。
    *   **生成式/模型化方法**：Decision Transformer (DT), Trajectory Transformer (TT), MOPO, MOReL, MBOP, 以及基于扩散规划的 Diffuser。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **推理开销**：由于在推理的每一步都需要进行 $E$ 次梯度更新（实验中通常涉及多条轨迹的并行采样和反向传播），该方法的推理延迟显著高于传统的单次前向传播策略。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 D4RL 中最核心的 24 个任务，每个任务均在 10 个不同的随机种子下运行并取平均值，提供了标准差。
*   **充分性与公平性**：
    *   实验对比了从传统值函数方法到最新的扩散规划方法，覆盖面广。
    *   通过直接对比“预训练 ReBRAC”与“推理优化后的 ReBRAC”，清晰地展示了推理时自适应带来的增益。
    *   **客观性**：作者诚实地指出了在某些简单任务（如 random 级别）中，推理优化并未带来显著提升（标注为 $\star$）。

### 6. 主要结论与发现
*   **性能提升**：MPCwDWM 在 MuJoCo 任务上平均得分 85.33，优于所有对比基准（如 SAC-RND 的 82.6）。
*   **复杂任务表现优异**：在极具挑战性的 AntMaze Large 任务中，性能提升尤为显著（例如从 51 提升至 66），证明了世界模型在长程规划中的价值。
*   **策略精炼有效性**：实验证明，即使是已经很强的离线策略，通过在推理时利用局部动力学信息进行微调，依然能获得统计学上的显著收益。

### 7. 优点与亮点
*   **端到端可微性**：将扩散模型采样过程转化为可微计算图，解决了扩散模型与策略优化结合时的梯度流断裂问题。
*   **局部信息利用**：不同于全局 Q 函数，世界模型和奖励函数是局部对象，更容易从离线数据中准确学习，MPC 机制有效地利用了这种局部准确性。
*   **即插即用**：该框架可以作为现有任何离线 RL 算法（如 ReBRAC, IQL）的增强插件。

### 8. 不足与局限
*   **计算延迟**：推理时的多次梯度迭代极大限制了其在对实时性要求极高的场景（如高频机器人控制）中的应用。
*   **模型误差累积**：虽然使用了扩散模型，但在长步数（$H$ 较大）的想象展开中，世界模型的预测偏差仍可能导致策略优化向错误方向偏移。
*   **算力细节缺失**：缺乏对训练和推理成本的量化分析，难以评估其性价比。
*   **未来改进方向**：作者提到未来需探索多步生成模型或流匹配（Flow Matching）技术来减少推理时的计算开销。

（完）
