---
title: "ThinkJEPA: Empowering Latent World Models with Large Vision-Language Reasoning Model"
title_zh: ThinkJEPA：利用大视觉语言推理模型赋能潜世界模型
authors: "Haichao Zhang, Yijiang Li, Shwai He, Tushar Nagarajan, Mingfei Chen, Jianglin Lu, Ang Li, Yun Fu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22281v1"
tags: ["query:1"]
score: 9.0
evidence: 用于预测未来世界状态的潜空间世界模型
tldr: ThinkJEPA 提出了一种结合大视觉语言模型（VLM）与潜空间世界模型（JEPA）的新框架。针对 JEPA 在长程语义捕捉上的不足和 VLM 在细粒度预测上的局限，该方法通过双时间路径设计，利用 VLM 的长程推理能力引导 JEPA 的密集帧动力学建模。实验证明，该方法在手部操作轨迹预测任务中显著优于现有基准，提升了长程预测的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22281v1/fig-001.webp\", \"caption\": \"Table 3. Comparison with EgoDex trajectory prediction baselines on EgoDex. We compare ThinkJEPA against the trajectory prediction baselines reported in EgoDex, including decoder-only and encoder-decoder architectures with Behavior Cloning, DDPM, and Flow Matching. ThinkJEPA achieves the best ADE/FDE among all compared methods.\", \"page\": 13, \"index\": 1, \"width\": 725, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2603.22281v1/fig-002.webp\", \"caption\": \"Table 2. Ablation studies. We vary the VLM token sources and the thinker module. Encoder denotes encoder tokens, AR denotes autoregressive tokens, and VJ denotes the V-JEPA predictor; No-Th removes the thinker module. We abbreviate latent metrics as FD (feature distance), SL1 (SmoothL1), and CD (cosine distance). Single seed (42), best-epoch selection by minimum validation ADE.\", \"page\": 13, \"index\": 2, \"width\": 725, \"height\": 211}, {\"url\": \"assets/figures/arxiv/2603.22281v1/fig-003.webp\", \"caption\": \"Table 1. Quantitative comparison across datasets. We report trajectory metrics (ADE/FDE/Acc) and latent forecasting metrics (FD/SL1/CD). FD/SL1/CD denote V-JEPA feature distance, latent SmoothL1, and latent cosine distance, respectively. All values are reported with three decimal places.\", \"page\": 12, \"index\": 3, \"width\": 725, \"height\": 211}, {\"url\": \"assets/figures/arxiv/2603.22281v1/fig-004.webp\", \"caption\": \"Table 11. Key architectural hyperparameters and tensor dimensions.\", \"page\": 20, \"index\": 4, \"width\": 527, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2603.22281v1/fig-005.webp\", \"caption\": \"Fig. 1. Overall Architecture of ThinkJEPA. ThinkJEPA couples a dense JEPA branch for fine-grained latent dynamics modeling with a uniformly sampled VLMthinker branch that provides long-horizon semantic guidance. The VLM guidance— including visual tokens from the ViT visual tokenizer and intermediate hidden states from the language model—is distilled by a pyramidal representation extraction module and injected into the V-JEPA predictor via layer-wise modulation. Concretely, guidance derived from language-model layers {L0, . . . , LN} is mapped to modulation parameters for predictor layers {T0, . . . , TK}. The predicted future latents are concatenated with past teacher latents to form the full latent sequence, which is then fed into a task head to produce downstream trajectory predictions.\", \"page\": 5, \"index\": 5, \"width\": 717, \"height\": 254}]"
motivation: 现有的潜空间世界模型难以捕捉长程语义，而视觉语言模型虽具备强语义推理能力，但在处理密集、细粒度的动作预测时存在局限。
method: 提出双时间路径框架，结合密集采样的 JEPA 分支与均匀采样的 VLM 推理分支，并通过层次化金字塔模块提取 VLM 特征以引导潜空间预测。
result: 在手部操作轨迹预测实验中，ThinkJEPA 的表现优于纯 VLM 基准和纯 JEPA 预测器，展现出更强的长程预测能力。
conclusion: 通过将 VLM 的高层语义推理与 JEPA 的底层动力学建模相结合，可以有效提升潜空间世界模型在复杂任务中的长程预测性能。
---

## 摘要
潜世界模型（如 V-JEPA2）的最新进展在根据视频观测预测未来世界状态方面展现出了巨大潜力。然而，基于短观测窗口的密集预测限制了时间上下文，并可能使预测器偏向于局部的低级外推，从而难以捕捉长程语义并降低下游效用。相比之下，视觉语言模型（VLM）通过对均匀采样的帧进行推理，提供了强大的语义基础和通用知识，但由于计算驱动的稀疏采样、将细粒度交互状态压缩为面向文本表示的语言输出瓶颈，以及在适配小型动作条件数据集时的数据分布不匹配，它们并不适合作为独立的密集预测器。我们提出了一种 VLM 引导的 JEPA 风格潜世界建模框架，该框架通过双时间路径将密集帧动力学建模与长程语义引导相结合：一个用于细粒度运动和交互线索的密集 JEPA 分支，以及一个具有更大时间步长、用于提供丰富知识引导的均匀采样 VLM “思考者”（thinker）分支。为了有效地迁移 VLM 的渐进式推理信号，我们引入了一个分层金字塔表示提取模块，将多层 VLM 表示聚合为与潜预测兼容的引导特征。在手部操作轨迹预测上的实验表明，我们的方法优于强 VLM 基线和 JEPA 预测器基线，并产生了更稳健的长程展开（rollout）行为。

## Abstract
Recent progress in latent world models (e.g., V-JEPA2) has shown promising capability in forecasting future world states from video observations. Nevertheless, dense prediction from a short observation window limits temporal context and can bias predictors toward local, low-level extrapolation, making it difficult to capture long-horizon semantics and reducing downstream utility. Vision--language models (VLMs), in contrast, provide strong semantic grounding and general knowledge by reasoning over uniformly sampled frames, but they are not ideal as standalone dense predictors due to compute-driven sparse sampling, a language-output bottleneck that compresses fine-grained interaction states into text-oriented representations, and a data-regime mismatch when adapting to small action-conditioned datasets. We propose a VLM-guided JEPA-style latent world modeling framework that combines dense-frame dynamics modeling with long-horizon semantic guidance via a dual-temporal pathway: a dense JEPA branch for fine-grained motion and interaction cues, and a uniformly sampled VLM \emph{thinker} branch with a larger temporal stride for knowledge-rich guidance. To transfer the VLM's progressive reasoning signals effectively, we introduce a hierarchical pyramid representation extraction module that aggregates multi-layer VLM representations into guidance features compatible with latent prediction. Experiments on hand-manipulation trajectory prediction show that our method outperforms both a strong VLM-only baseline and a JEPA-predictor baseline, and yields more robust long-horizon rollout behavior.

---

## 论文详细总结（自动生成）

这是一份关于论文《ThinkJEPA: Empowering Latent World Models with Large Vision-Language Reasoning Model》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何结合潜世界模型（Latent World Models）的细粒度动力学建模能力与大视觉语言模型（VLM）的高层语义推理能力，以实现更准确、更具长程鲁棒性的未来状态预测。
*   **研究背景**：
    *   **JEPA（如 V-JEPA2）的局限**：通常依赖短观测窗口的密集采样，虽能捕捉微小运动，但缺乏长程语义上下文，容易陷入局部物理外推，且缺乏对通用知识的理解。
    *   **VLM 的局限**：虽然擅长高层推理，但受限于计算成本通常采用稀疏采样，且其语言输出瓶颈会将连续的物理交互状态压缩为离散文本，导致在处理细粒度、高帧率的物理预测（如手部操作）时表现不佳。

### 2. 方法论：ThinkJEPA 框架
核心思想是建立一个**双时间路径（Dual-Temporal Pathway）**架构，将 VLM 作为“思考者（Thinker）”来引导 JEPA 预测器。

*   **双时间感知野采样**：
    *   **密集 JEPA 分支**：对短窗口内的帧进行全采样，捕捉高频运动和接触变化。
    *   **均匀 VLM 分支**：在长视频跨度内均匀采样少量帧，以获取全局上下文和事件级语义。
*   **层次化金字塔表示提取**：
    *   不只使用 VLM 的最后一层特征（因为深层特征过于偏向文本生成），而是聚合了 VLM 的**视觉 Tokenizer 输出**和**多个中间隐藏层（如第 0, 4, 8...27 层）**的特征。
    *   这种金字塔结构保留了视觉推理的痕迹和空间敏感性。
*   **层级引导注入**：
    *   使用 **FiLM（特征线性调制）** 技术，将提取的 VLM 引导特征注入到 JEPA 预测器的每一层 Transformer Block 中。
    *   公式化表达为：$\hat{F}_{fut} = g(F_{past}; \phi(v_u), p)$，其中 $v_u$ 是均匀采样视频，$\phi$ 是金字塔特征提取函数，$p$ 是文本提示。

### 3. 实验设计
*   **数据集与场景**：
    *   **EgoDex**：大规模第一视角灵巧操作基准，包含 3D 手部和手指姿态标注。
    *   **EgoExo4D**：大规模多模态技能活动数据集，包含同步的内外视角视频及 3D 人体/手部姿态。
*   **任务目标**：3D 手部轨迹预测（通过预测未来潜表示并解码为轨迹）。
*   **对比方法（Benchmark）**：
    *   **Qwen3-VL Thinking (VLM-only)**：仅使用 VLM 特征进行预测。
    *   **V-JEPA Predictor (JEPA-only)**：标准的潜空间预测器，无 VLM 引导。
    *   **传统轨迹预测基准**：包括基于 Behavior Cloning、DDPM 和 Flow Matching 的 Transformer 架构。

### 4. 资源与算力
*   **模型规格**：视觉主干使用 V-JEPA-L (ViT-Large)，VLM 使用 Qwen3-VL (Thinking)。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了训练时的超参数：Batch Size 为 14（训练）/ 6（评估），学习率为 $10^{-3}$，预测器学习率为 $10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型数据集上进行了主实验对比。
    *   进行了**递归展开（Recursive Rollout）**实验，测试了从 4 步到 32 步的长程预测稳定性。
    *   **消融实验充分**：涵盖了 VLM Token 来源（编码器 vs 自回归 Token）、VLM 层级选择（单层 vs 多层金字塔）、采样步长、注入机制（FiLM vs Cross-attn vs AdaLN）等。
*   **评价**：实验设计较为客观，通过对比“纯 VLM”和“纯 JEPA”基线，清晰地证明了双分支结合的增益。

### 6. 主要结论与发现
*   **性能提升**：ThinkJEPA 在轨迹预测精度（ADE/FDE）和潜表示预测质量上均显著优于单一分支模型。
*   **长程鲁棒性**：在递归展开实验中，随着预测步长增加，ThinkJEPA 的领先优势扩大，说明语义引导能有效抑制误差累积。
*   **特征互补**：VLM 的中间层特征对物理预测至关重要，仅靠最后一层语义特征效果有限。

### 7. 优点与亮点
*   **架构创新**：巧妙解决了密集动力学（JEPA）与稀疏语义（VLM）之间的时间分辨率矛盾。
*   **金字塔提取**：意识到 VLM 深层特征的“语言瓶颈”问题，通过多层聚合保留了视觉推理细节。
*   **通用性**：该框架不改变 JEPA 的潜空间预测本质，使其易于扩展到其他具身智能任务。

### 8. 不足与局限
*   **计算开销**：虽然 VLM 特征可以缓存，但在推理时同时运行 VLM 和 JEPA 预测器会显著增加内存和计算负担。
*   **任务覆盖**：目前主要集中在手部轨迹预测，尚未在更复杂的全身运动或多物体交互场景中广泛验证。
*   **实时性**：论文未讨论该模型在实时控制系统中的延迟表现。

（完）
